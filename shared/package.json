{
  "name": "@finaroda/scoring-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "scoring-engine.js",
  "exports": {
    ".": "./scoring-engine.js",
    "./scorer.js": "./scorer.js",
    "./scoring-engine.js": "./scoring-engine.js",
    "./package.json": "./package.json"
  },
  "description": "Shared deterministic scoring engine (levels + scorer). Imported by the personal tool and the SaaS scan core (SPEC §6.1).",
  "scripts": {
    "test": "node --test"
  }
}
