# VERSIONS — FINARODA SaaS

> מעקב גרסאות (SemVer). כל bump נרשם כאן. החדש למעלה.

| גרסה | תאריך | תקציר | Commit |
|------|-------|--------|--------|
| v0.18.1 | 2026-07-16 | MINOR — UPGRADE: **F16b Outcome Narratives**. Extends the F16 engine with RESOLVED-SCENARIO states rendered when a revealed dashboard scenario is opened. New pure `resolveOutcomeNarrative` maps `journal_scenarios.status` → R-state reading ONLY existing resolution-record fields (status, r_result, coin, direction, score) — mentor HARD RULE: no new financial computation, no new flag logging. **R1 target_hit / R2 stop_hit / R3 expired_flat ship LIVE**; **R4 save_confirmed / R5 save_missed are BUILT but gated behind `FEATURE_ARENA`** (backend config + `NEXT_PUBLIC_FEATURE_ARENA`, both default OFF) → activate with F17 after lawyer clearance. R2 frames loss as variance-not-error; R5 never regret. **Foreseeability:** the "could it have been foreseen" line renders affirmative ONLY from a logged decision-time flag; NO such flag exists in `journal_scenarios` today, so R2/R3 always append the honest "nothing in the data marked this in advance" (affirmative template carries a `{foresee_flag}` placeholder → structurally cannot render without a flag; no manufactured hindsight). Locked `market_narratives.json` extended (`resolved_states` + `foreseeability`, root+frontend byte-identical, same drift/em-dash/imperative/DRAFT/source_ref governance + resolved-state + zero-XP tests). Dashboard revealed rows now expandable (OUTCOME NOTE card, concept tooltips wired); gated save/skip rows do not expand while OFF (fallback). Zero new XP sources. pytest **208/208** (+5) · frontend unit **87/87** (+14) · shared **14/14** · tsc/eslint clean · red-lines green. APP_VERSION → 0.18.1. | ad87f76 |
| v0.18.0 | 2026-07-15 | MINOR — UPGRADE: **FX4 coin-identity gating + F16 Market Narrative**. **FX4** (founder-approved, was escalated in v0.17.2): per-plan coin allowlist, DB-backed (`coin_access`, mig 037: plan PK + coins JSON + wildcard flag), admin-editable live without a deploy (read per request). Seed free=[LINK,AVAX] / basic=[LINK,AVAX,SOL,ADA,DOGE] / pro=wildcard (auto-includes future coins); trial=Pro. Server-authoritative: `/api/scan/events` rejects a managed-universe coin outside the allowlist with **403 COIN_GATED** (after the COUNT gate, before the daily cap). **Universe-only rule**: identity gating applies only to `SCAN_UNIVERSE_BASE` coins, so the count-gate red-line law (proven on synthetic C{i}USDT) stays orthogonal and untouched. New `GET /api/scan/coin-access` (allowed + locked→unlock-plan map) drives the selector: locked coins stay VISIBLE with 🔒 + "Available on <plan>", tap routes to /subscribe (show the door). New admin "Coin access" section (per-plan checklist + Pro wildcard toggle, audited to admin_events). History unaffected (gating is new-scans only). **F16** (proposed 15/07, mentor-amended): deterministic Market Narrative card, 6 states (S1 regime_blocked_spike / S2 no_setups_quiet fallback / S3 transition_flicker / S4 pass_with_context / S5 watch_only / S6 daily_limit_reached), first-match-wins resolver reading ONLY existing scan-payload fields (B3, NO new computation). **S3 DEGRADED** (no reversal/short-EMA-reclaim flag in payload → positive ema7 slope proxy). Card after ring/list before Blueprint; S6 renders INSIDE the 429 quota screen (B2); Free=paid. Locked-file `market_narratives.json` (root+frontend byte-identical, drift-guard + no-em-dash + no-imperative + DRAFT + source_ref governance tests); 2-3 date-rotated DRAFT variants/state; concept terms wired to real tooltips. **Founder ruling:** to keep AC4 (red-line suites untouched+green) with the FX4 free-seed, the two red-line suites that scanned BTC-on-free (reveal, xp) had the incidental coin swapped BTC→LINK (an allowlisted free coin), all assertions preserved. pytest **203/203** (+18) · frontend unit **73/73** (+11) · shared **14/14** · tsc/eslint clean · red-lines green. APP_VERSION → 0.18.0. | c908eae |
| v0.17.2 | 2026-07-15 | PATCH - FIX ROUND (founder validation, pre-Stage-8). **FX1 (P0):** new users never saw onboarding — `/login` (magic-link + DEV SIGN-IN) + `/verify` routed straight to `/scan`, bypassing the in-flow signup; added `routeAfterAuth()` (reads `/api/auth/me.onboarding_completed`) wired into login/verify + a `/scan` safety-net guard → unfinished users go to `/onboarding` (300 XP still granted once). **FX2 (P0):** no dead end after the Free scan — the 429 quota screen + server `DAILY_SCAN_LIMIT` already existed (tested); real gap was navigation, so added an explicit "Scan" entry to the hamburger (logo→/scan already worked). **FX3 (P0):** rebuilt the Recent-scans detail — removed the FORBIDDEN words ENTRY/SL/TP, now renders the full Trading Blueprint in canonical terms (Mathematical Trigger Point / Calculated Risk Level / Dynamic Risk Level / Calculated Target Level + Risk:Reward + TIMING VERIFIED/WATCH header) + BlueprintChart (plan-gated layers). Setup-time levels only (already-shown, not withheld); reveal/outcome stays on F3. **FX4 (P1):** per-plan coin-IDENTITY gating is NOT in spec (only COUNT 2/5/10) — not invented, escalated to founder. **FX5 (P1):** Settings gains a PLAN & BILLING card (plan/trial/subscribe/cancel); slow load fixed by parallelizing the profile+prefs fetches (was a waterfall behind `/me`; endpoints already indexed, no N+1). **FX6 (P1):** app-shell anchor — reading columns get `minHeight:100vh` (short screens stop collapsing) + `maxWidth 480`; Academy filter row wraps (no cut-off). **FX7 (P2):** green unread dot on the hamburger, clears on bell open (`finaroda:notifications-read`). pytest **185/185** (+3) · tsc clean · eslint clean · frontend unit **62/62** (+4) · no SL/TP/ENTRY guard green · red-lines untouched. APP_VERSION → 0.17.2. | 9bb629b |
| v0.17.1 | 2026-07-14 | PATCH - VALIDATION V1: full-product ATP + fix round (pre-Stage-8 quality gate). Built the Acceptance Test Procedure as CODE (v0.10.x-v0.17.0) at CI/CD depth with **dedicated red-line invariant suites**: reveal-gating (9), XP-economy (7, incl. a static closed-source-list + no-spend guard over all backend), entitlements/permission-matrix (7), money-agorot (6, static float-guard), secret/Cardcom lint (2), + 4 E2E journeys. Executed on a **fresh environment** (clean venv + `pip install -r`, clean install, `next build`) — all green. **FINDING-1 (P1, fixed):** admin `suspend` was a silent no-op (`get_current_user` never checked `suspended_at`) -> a suspended account kept full access; fixed with a single server-authoritative 403 ACCOUNT_SUSPENDED chokepoint + regression TC-V1-ENT-09. Not a reveal/XP/money leak. **em-dash copy guard hardened** (pre-approved): strips JSX `{/* */}` + block/line comments before scanning, meta-tested — the lint is now trustworthy. No P0. 2 P2-debt registered (require_active_trial latent; hardening rate-limit). Deliverables: validation/ATP_V1.md + validation/VALIDATION_REPORT_2026-07-14.md + 6 test files. pytest **182/182** (+36) project-venv AND fresh-venv · tsc/eslint clean · frontend unit **58/58** · next build OK. APP_VERSION -> 0.17.1. | 1821eca |
| v0.17.0 | 2026-07-14 | MINOR - UPGRADE STAGE 4: Coupons + Referral (Stripe-native). **coupons** = Stripe Coupon + Promotion Code (percent OR fixed ILS, duration once = first charge only); admin console drives Stripe (create/list/deactivate, 403+audited) with a mirror row our side; redemptions synced from webhooks. **referral** = permanent per-user 8-char code + /r/<code> share link, bound once at signup (immutable, self-referral blocked by id+email); a referred friend's FIRST amount>0 invoice.paid earns the referrer ONE FREE MONTH via Stripe customer balance credit (one month of the referrer's current plan), or **banked** (referral_credits) for trial/free referrers and applied on their first paid checkout (resolved to the plan bought; credits stack). 100%-coupon first month (amount 0) does NOT trigger the reward. Reward idempotent via a single bound->rewarded transition. **D-S8:** zero-total invoices issue NO Israeli tax document (audit row instead). **C1:** mig 036 reshapes the empty dormant 003/004 tables (coupons mirror, coupon_applications->coupon_redemptions, referrals binding+reward, new referral_credits, subscription_events CHECK +6 audit types) - non-destructive, STOP S3 cleared (0 rows). **C2:** webhook verify now uses the official `stripe.Webhook.construct_event` (SDK) instead of hand-rolled HMAC (AC9). **D-S1 mechanism:** plan restriction enforced OUR-SIDE (validate before Checkout Session creation), not Stripe applies_to. **core/coupon_service.py** + **core/referral_service.py** + **api/admin_promotions.py** + **api/referral.py** + emails (reward earned / banked applied). Frontend: admin Coupons+Referrals sections, /r/[code] route, profile invite card, checkout promotion_code. XP untouched in all directions (AC8 asserted). No new env. pytest **146/146** (+18) · tsc/eslint clean · frontend unit **58/58** (+7) · em-dash guard clean. APP_VERSION -> 0.17.0. | a593f6f |
| v0.16.0 | 2026-07-14 | MINOR - UPGRADE STAGE 3R: PSP migration Cardcom -> Stripe (Stripe Checkout + Billing). **core/stripe_service.py** (replaces cardcom_service): hosted Checkout Session (DEV fallback = zero-network fake session), webhook signature verify (hand-rolled Stripe HMAC, SDK-free boundary) + event-id idempotency (`processed_webhook_events`) + dispatch of checkout.session.completed / invoice.paid / invoice.payment_failed / customer.subscription.updated / customer.subscription.deleted; cancel via Stripe cancel_at_period_end (end-of-period access kept + churn hook); trial stays OURS (card-free, not a Stripe trial, D-R2). Recurring + dunning are **Stripe Billing / Smart Retries** now - homegrown renewal/dunning DELETED; our failure/recovery/cancel emails still fire from the webhook (D-R4). **core/invoice_provider.py** (replaces cardcom_invoice): provider-agnostic Israeli tax document, MOCK default (offline) + documented interface for Green Invoice / iCount / EZcount (`INVOICE_PROVIDER`); Stripe invoices are NOT tax documents; doc type default -> `tax_invoice_receipt` (VAT LTD). **billing_state.py** survives as entitlement source of truth, fed only by webhooks; one edge added (active -> expired). API `/api/cardcom/*` -> `/api/billing/*`. mig 035 renames cardcom cols -> stripe_customer_id/stripe_subscription_id/card_last4/card_expiry, cardcom_tx_id -> stripe_reference, cardcom_response_json -> provider_response_json, cardcom_document_id -> provider_document_id; + `processed_webhook_events`. Money still agorot/ILS/VAT-inclusive. Idempotent `seed_stripe_prices.py`. STOP S1(ILS ok)/S2(no state add, one edge)/S3(no Stage-5 break) all cleared. AC7: zero cardcom in live code + zero live-key pattern (grep test). env NEW: FEATURE_STRIPE_LIVE, STRIPE_SECRET_KEY, STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, INVOICE_PROVIDER, STRIPE_SUCCESS_PATH, STRIPE_CANCEL_PATH; REMOVED all CARDCOM_*, FEATURE_CARDCOM_LIVE, DUNNING_RETRY_OFFSETS_HOURS. pytest **128/128** · tsc/eslint clean · frontend unit **51/51** · shared 14/14. APP_VERSION -> 0.16.0. | adb1e44 |
| v0.14.0 | 2026-07-14 | MINOR — UPGRADE STAGE 3: Live Billing (Cardcom) spine, sandbox-only (no live terminal). **מכונת-מצבים אחת** `core/billing_state.py` (D-B4): none/trial/active/past_due/cancelled/expired + מטריצת מעברים חוקיים (דוחה לא-חוקי, `IllegalTransition`), `effective_tier` נגזר-מ-state בלבד; כל שינוי סטטוס עובר `apply_transition` (guard + audit ל-`subscription_events`). **מסמכי חיוב** `core/cardcom_invoice.py` (D-B3): מסמך receipt/invoice_receipt לכל חיוב מוצלח, סוג-מסמך CONFIG (`system_settings.billing_document_type`), טבלה `billing_documents` (mig 034), idempotent per-tx; **offline=MOCK אפס-רשת** (S3, אין חשבון Cardcom). **checkout** (webhook HMAC, D-B8): אישור צד-שרת בלבד → active דרך state machine + מסמך + מייל אישור; idempotent by deal-id, redirect לבד לא מפעיל, חתימה פגומה נדחית. **חיוב חוזר + dunning** (D-B5): `run_renewal_batch` מחייב active-due + past_due-retry-due; כשל → past_due, retry ב-+24h/+72h (config `DUNNING_RETRY_OFFSETS_HOURS`), מייל + פעמון לכל כשל, מיצוי → expired → Free; recovery past_due → active; idempotent (ריצה כפולה = 0 חיובים). **ביטול end-of-period** (D-B6): status → cancelled, גישה עד תום התקופה, cron `drop_cancelled_to_free` → Free; churn survey (`/api/churn/survey`) מחווט מ-ChurnSurvey; ביטול-כפול בטוח. **cron** `POST /api/cron/billing` (X-Cron-Secret, D-B9). **כסף = agorot ints** end-to-end (D-B10). **D-B7 forward-compat:** `coupon_code`/`referral_source` על charges + documents (nullable, inert). Frontend: `BillingBanner` (past_due/cancelled/expired) ב-scan + settings, cancel dialog, מחיר ₪ + הערת VAT. STOP: **S1** (אין credentials → mock + רשימה ל-HANDOFF), **S2** (אין קונפליקט: ה-CHECK הקיים מכסה את D-B4, רק איות trial/cancelled), **S3** (סוג-מסמך CONFIG + שאלות לרו"ח ב-HANDOFF). env חדש: `BILLING_PERIOD_DAYS`, `DUNNING_RETRY_OFFSETS_HOURS`. pytest **129/129** (+17 Stage-3) · tsc/eslint clean · frontend unit **51/51** (+6). APP_VERSION→0.14.0. | 24e5097 |
| v0.13.0 | 2026-07-14 | MINOR — UPGRADE STAGE 6: Academy 2.0. מעבר מרשימה קשיחה (`_MODULES`) ל-**טבלת DB `academy_lessons` (mig 033)**. (A) **card grid** רספונסיבי במקום רשימה (`academy/page.tsx`, maxWidth 1040 + grid auto-fill; כרטיס=כותרת/תיאור/משך/תג-סוג/מצב-נעילה/הושלם). (B) **חיפוש+פילטרים** צד-לקוח (`lib/app/academy.ts` — `filterLessons`/`lessonState`, AND על title/description/tags × type × locked/unlocked/completed, unit-tested). (C) **וידאו** — `content_type='video'` + URL של YouTube/Vimeo בלבד (`validate_video_url` מנרמל ל-embed, דוחה אחר 400), נגן lazy (`VideoEmbed.tsx`, iframe רק בלחיצה). (D) **ניהול אדמין** (`api/admin_academy.py` + `AcademyAdmin.tsx`, סקשן "academy") — create/edit/reorder(up-down)/archive/restore/tags/gating, admin-only→403, audited ל-`admin_events`. **שער-כפול (D-AC1, `core/academy_gate.py`):** `min_plan`(free/basic/pro) + `min_rank`(0/1000/3000/8000), שניהם חייבים לעבור; rank=STATUS לא הוצאת-XP; `lock_reason` בשפה פשוטה. **תוכן נשלט-שרת (D-AC7):** רשימה=מטא-דאטה בלבד, `GET /api/academy/{slug}` מחזיר 403 לשיעור נעול (אין דליפת body/video). **השלמה/XP ללא שינוי:** +100 חד-פעמי ב-`xp_events` (source=`academy_lesson`, ref=`slug`); מיגרציה purely-additive שומרת כל השלמה קיימת לפי slug (S3). archive-not-delete: XP לא נשלל. seed=12 השיעורים הקיימים (slug==module_id ישן, body מ-concept JSON, 3 stubs awards_xp=0). STOP S1/S2/S3 נבדקו, אף אחד לא חסם. תוכן שיעורים = משימת נדב. pytest **112/112** (+12) · tsc/eslint clean · frontend unit **45/45** (+8, כולל viewport.regression) · shared 14/14. APP_VERSION→0.13.0. | 17bd07b |
| v0.12.0 | 2026-07-14 | MINOR — UPGRADE STAGE 7: Admin v1.1 + Sentry + Ticket Breadcrumbs. **טבלת משתמשים v1.1** (`api/admin.py`): עמודות עשירות (scans total/week, active-days 7d/30d כ-**ADMIN ANALYTICS בלבד**, XP, rank server-side מ-`core/ranks.py`, referrals placeholder 0, churn flag) + פילטרים צד-שרת AND (plan/status/signup-range/last-active-range/min_scans, URL-encoded shareable) + **CSV export** (`/users/export.csv`, StreamingResponse, admin-only). **Churn survey** (`api/churn.py` → `POST /api/churn/survey` → `churn_reasons` mig 006) מ-Settings ("Cancel plan or leave") + admin `/api/admin/churn` + flag + סינון churned. **Sentry** env-gated: backend `core/monitoring.py` (scrub PII, user.id בלבד, `before_send`), frontend `lib/sentry.ts` + `src/instrumentation(-client).ts` (dynamic import, `@sentry/nextjs`). **Breadcrumbs** (`lib/breadcrumbs.ts` ring-20 → route/scan/api_error/notif hooks → ticket payload → sanitizer `core/breadcrumbs.py` allowlist → mig 032 `support_tickets.breadcrumbs` → admin timeline). RED LINE: אין ערך תוצאה ב-breadcrumbs (test). + drift-fix: em-dashes הוסרו מ-welcome/beta emails. STOP S1/S2 שניהם לא-חוסמים. env: `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN`. pytest **100/100** (+14) · tsc/eslint clean · frontend unit **37/37** (+7) · shared 14/14. APP_VERSION→0.12.0. | 617e4b3 |
| v0.11.0 | 2026-07-14 | MINOR — UPGRADE STAGE 5: Notifications + Bell + Preferences + Real Resend. **פיד in-app** (`notifications`, mig 031) עם פעמון בהמבורגר (badge cap 9+, newest-first, mark-read-on-open, unread שורד refresh · server-authoritative), **prefs per-user** (`notification_prefs`: inapp/sound/vibration/email_product/email_broadcast) חוצה-מכשירים, **Resend אמיתי** ב-`core/email.py` (renderers טהורים + `_send_or_log` עם DEV console-fallback, אפס קריאות רשת בבדיקות), **3 זרמי אימייל**: day-11 trial reminder (חלון datetime half-open, boundary day-10/11/12, idempotent), **reveal-teaser אפס-ערכי-תוצאה** (D-N5, dedup דרך `journal_scenarios.teaser_sent_at`), admin broadcast (preview opted-in + confirm step + **unsubscribe חתום חובה**, שולח רק לאופט-אין). API חדש: `/api/notifications` (feed/read/prefs), `/api/email/unsubscribe` (HMAC, ללא login, idempotent), `/api/cron/notifications` (X-Cron-Secret, idempotent). mig 031 גם משנה טבלת `notifications` מתה מ-mig 005 ל-`notifications_legacy_outbox` (לא-הרסני). STOP S1(trial=14) אושר · S2(domain) לא-חוסם ב-DEV. env חדש: `CRON_SECRET`. pytest **86/86** (+15) · tsc/eslint clean · frontend unit **30/30** (+6) · shared 14/14. APP_VERSION→0.11.0. | b714ca8 |
| v0.10.1 | 2026-07-13 | PATCH — RESPONSIVE PASS (phone ~390px + desktop, mandate של נדב). מסכי המוצר כבר mobile-first (עמודת maxWidth) — ה-offender היחיד היה **קונסולת האדמין** (rail 200px + פאנלים 280/340/300 + grids 5-טורים). נוסף `useIsMobile` hook; globals.css safety layer (`body overflow-x:hidden`, `main padding clamp(0.9rem,4vw,2rem)`, `img/svg/table max-width:100%`). Admin responsive: rail→**פס טאבים עליון sticky+נגלל**, Overview grid→`auto-fit minmax(140px)`, Users/Tickets→**master/detail swap** (Users=כרטיסים בטלפון), Broadcast→stack, Notifications→overflowX + **טור ראשון sticky**, Settings locked→column; ה-main של admin דורס את חוק main הגלובלי; desktop=B7 רוחב-מלא. Copy: dashboard reveal caption תלוי-tier (Free="Revealed on tomorrow's scan" · paid="on your next scan"). **Harness רגרסיה** `frontend/tests/viewport.regression.test.ts` (structural-lint ב-node --test, בחירת נדב — ללא Playwright; רשימת דפוסים ניתנת-להרחבה). pytest 71/71 · tsc/eslint clean · frontend unit **24/24** (18+6). APP_VERSION→0.10.1. | 3c58c38 |
| v0.10.0 | 2026-07-13 | MINOR — FIX-R3R4 (validations 3+4). **Decision A: 3 plans** (Advanced הוסר) Free/Basic/Pro coins 2/5/10, prices basic ₪59 / pro ₪149 **PENDING-ACCOUNTANT**; mig 029 (advanced→basic + retune settings), plans/entitlements/cardcom/admin/academy + /api/plans מחזיר 3. **Bug 3: אכיפת scans/day** צד-שרת ב-/api/scan/events (429 DAILY_SCAN_LIMIT, Free=1/יום, בתשלום=unlimited, admin-editable) + friendly limit state בקליינט. **Bug 4: trial** — "Start 14 days" (onboarding S11 + Subscribe) קורא POST /api/cardcom/trial ונוחת /scan עם TRIAL chip; day-11 → notifications_log (idempotent); expiry→Free. **Bug 2: XP** נמענק ב-S9 (call-sign submit) + call-sign נשמר, לא תלוי ב-S11. **Bug 1: flash** — scanning overlay נשאר עד ש-reveal חוזר (S2/S8/S10). **Bug 5:** SEE PLANS→חזור משחזר RESULTS (sessionStorage). **Bugs 6/7/8:** S1 כותרת 2-שורות, S8 placeholders עקביים, כפתור "N COINS". **Decision B:** דף Recent scans (GET /api/scan/history[/{id}]). **Decision C:** בחירת מטבעות בתוך מכסת הפלאן. **Decision D:** why-not בתשלום מציג מספרים אמיתיים (price vs EMA200 % / EMA7 slope / volume). **Decision E:** ברירות מחדל Lens=Full/Style=Balanced מסומנות. **UX:** לוגו→/scan, greeting לפי call-sign, dashboard "How is R measured?", Profile≠Settings, login DEV SIGN-IN + DEV_RETURN_MAGIC_LINK guard, admin back-to-app, tickets מצרפים app_version (mig 030) + 20 אירועים אחרונים בתצוגת אדמין. pytest 71/71 · frontend unit 18/18 · shared 14/14 · tsc/eslint clean. | e2e49b8+b852dd7 |
| v0.9.1 | 2026-07-13 | PATCH — DOCS-SYNC: יישור PRD/SPEC/UX למצב הממומש אחרי Package B (v0.8.x–v0.9.0). PRD F3 = מודל `journal_scenarios` (‏pass/no_setups_day, WATCH לא-תרחיש, resolution צד-שרת, reveal-gating) + הגדרת **CAPITAL SAVES** (trigger-never-filled, r=0) והערה ש-non-passer SAVE = הרחבה עתידית. SPEC §5.9 חדש (journal_scenarios/ticket_replies/notifications_log/user_settings mig 028 + admin_events mig 006 + endpoints entitlements/plans + מפתחות system_settings chart_layers/scan_coins_free/scans_per_day). SPEC §6.1 = `findRecentSwingLevels` canonical S/R equivalence-tested, `computeRangeLevels` הוסר. סימון SHIPPED (F1/F1c/F2/F3/F5/F6/F7/F9/F10/F11/F13/F14/F15/E7b) + charts=in-app SVG. open items = follow-ups מ-HANDOFF (resolve cron, call-sign, email stubs, fonts). +ATP TC-DOCS-SYNC01. docs only, אפס קוד. pytest 66/66. | <hash> |
| v0.9.0 | 2026-07-13 | MINOR — Package B phase 2 (B4 dashboard + B5 profile + B6 academy + B7 admin). B4: `journal_scenarios` (mig 028) מכל סריקה (PASS→תרחיש · יום-דילוג→no_setups_day · WATCH לעולם לא תרחיש), resolution job צד-שרת (Bybit klines · trigger→target/risk/7-day · r_result היפותטי R-בלבד), **reveal-gating** — תוצאה מחושבת בשרת ולא נכללת בשום payload עד הסריקה הבאה (badge=ספירה בלבד, שורות לא-חשופות ללא דאטת תוצאה ב-DOM · regression S10) · +25XP צפייה (idempotent). B5: `/api/profile` call-sign+סולם דרגות(1000/3000/8000)+4 מקורות XP+הגדרות Lens/Risk(display+geometry)+sign-out. B6: 12 מודולים=12 academy-ids, seed מ-`what`, deep-link tooltip #anchor, +100/שיעור רק לתוכן-אמת(≥3 מונחים) stubs=0, plan-gating + מודולי בונוס לפי דרגה. B7: קונסולת אדמין (require_admin→403), overview דאטת-אמת/MRR/churn, users+override(audit→admin_events), tickets queue+reply(email stub), settings editor(score-gate/card-off LOCKED), broadcast+audience→banner in-app(לא מכסה SCAN/disclaimer), notifications log(day-11+reveal teaser). mig 028 (journal_scenarios/ticket_replies/notifications_log/user_settings + broadcast audience). pytest 66/66 · frontend unit 18/18 · shared 14/14 · tsc/eslint clean · next build 20/20 · em-dash 0. | <hash> |
| v0.8.0 | 2026-07-13 | MINOR — Package B phase 1 (B1 scan + B2 subscribe + B3 nav). B1: full scan-screen replacement — pre-scan controls (E9 Horizon SWING/POSITION-locked, display-only Lens, geometry-only Risk Style, RED-LINE caption), locked 4-step scan, ring/list results, server-authoritative first-scan-of-day XP (+50), Chart Standard v1 in every Blueprint with E7 layer gating (Free=EMA200 / paid=all+SEE PLANS), E7b every coin tappable → same chart + plain-language "why not" (locked concept label, no score/weight), F1b empty state (no scan CTA), new-scan→controls. B2: 4-plan comparison table (TC-J-002, Free first), D1 no-card trial CTA + 3 trust shields, prices/coins from system_settings via /api/plans. B3: unified header (LevelMeter chip) + hamburger drawer (Dashboard/Profile/Academy/Settings) + Report-a-problem→ticket. Backend: /api/scan/entitlements (binding), coin-limit gating on /events (403), first-scan XP (idempotent/day), /api/plans, /api/support/tickets, /api/cardcom/trial; mig 027 (per-plan entitlements in system_settings). Shared engine: ported personal-tool findRecentSwingLevels as canonical S/R (deleted lib/onboarding/levels.ts, repointed all charts) + equivalence test. pytest 55/55 · frontend unit 14/14 · shared 14/14 · tsc/eslint clean · next build clean · em-dash 0. | <hash> |
| v0.7.1 | 2026-07-13 | PATCH — F13 validation round 2 (polish). S0 LET'S START button (no auto-advance); signup flash fixed via render-gate + createOnce guard; header redesigned to a compact terminal LEVEL meter (hexagon rank badge + XP + progress, XP_ECONOMY ladder); tooltip `now` suppressed when context absent; chart header (symbol on its own badge, all charts); S8 pre-scan framing line + risk_price 0.1511 drawn + decluttered Trigger/Risk/Target labels + Why-PASS popover (stored checks); XP→LEVEL framing with level-up celebration gated to rank crossings only; S10 journal copy made unambiguous; S11 table overflow synced to frame. New frontend unit tests (node --test type-strip): level math, tooltip suppression, transition guard. pytest 43/43 · unit 7/7 · tsc/eslint clean · build 17/17. | <hash> |
| v0.7.0 | 2026-07-12 | MINOR — F13 validation round 1 + tooltip content. Concept Tooltips wired to concept_tooltips_content.json (46 terms, `now` template engine, mobile clamp/flip + X + tap-outside). Bugs: XP once-per-user-ever (mig 026 partial unique index, grant at completion, replay=0 regression); returning-user routing (completed → /scan, back never re-enters); signup flash removed; live "new scan" returns to controls; line-orphan control. Product: BUY/SELL → LONG/SHORT; two real-candle S1a branches (LONG fade / SHORT squeeze, E1 decision candle = 20 Jun surge, builder-asserted); no-em-dash copy rule + pytest lint. Chart Standard v1 (header, EMA labels, swing S/R, blueprint levels, event annotations→tooltips, candle-tap OHLC). pytest 42/42 · tsc/eslint clean · build 17/17. | <hash> |
| v0.6.0 | 2026-07-12 | MINOR — F13 "First 60 Seconds" onboarding. Episode engine (mig 023 episodes + real Bybit klines seed with empirical-truth assertions; E1 re-picked LINK→BTC 25/06), server-side outcome withholding (setup vs reveal), xp_events (mig 024, server-priced + idempotent, 300 total), onboarding_funnel_events (mig 025). Router api/onboarding.py (episodes/reveal/xp/funnel/complete). Frontend /onboarding: OnboardingFlow S0–S11+S1a, in-app SVG EpisodeChart (real klines, not recharts — avoids React-19 peer-dep), ConceptTooltip (placeholder content), XPMeter, Disclaimer every screen, SCAN vibrate+silent iOS fallback. pytest 39/39 · tsc/eslint clean · next build 17/17. | <hash> |
| v0.5.3 | 2026-07-11 | PATCH — DOCS: XP_ECONOMY.md v1.0 anchored + debt closed. UX §5 + PRD F5 tier fix ("מדייק"/Precise = XP threshold, **not** what-if outcomes; what-if quality = dashboard stat only) · PRD F6 links XP_ECONOMY (+100/lesson, ranks unlock bonus modules orthogonal to plan gates) · SPEC §5.6 new `xp_events` table (UNIQUE user+source+ref, server-side only, closed-list sources) · Onboarding §4/§8 debt marked closed · ATP TC-DOCS-XP01. Also fixed the identical contradiction in PRD F5 (instruction named only UX §5) — flagged for Nadav. Docs only, אפס קוד. | <hash> |
| v0.5.2 | 2026-07-11 | PATCH — DOCS: E9 Horizon selector. PRD F1c (SWING 1–7d active / POSITION weeks+ locked, "In validation. Unlocks when it earns it.") + V2 backlog (position engine) · UX §3/§8 pre-scan controls row · ROADMAP X1 · ATP TC-DOCS-E06. Honesty guardrail baked into AC2 (no position engine yet → "planned" not "in validation"; ⚠ open for Nadav). Docs only. | <hash> |
| v0.5.1 | 2026-07-11 | PATCH — DOCS: יישום section E של ALIGNMENT (11/07). E1 Concept Tooltip (F14) · E2 free-coin V2+"Learning mode" label · E3 טבלת השוואה חובה בעמוד Subscribe (F7/UX §6 + TC-J-002) · E5 hamburger nav (X1) · E6 SCAN navigator.vibrate+fallback (SPEC §6.2) · E7 Live Chart+overlays (F15, Free=chart+EMA200/paid=all) · E8 ticker banner REJECTED (PRD §17). Docs only, אפס קוד. | <hash> |
| v0.5.0 | 2026-07-09 | MINOR — D1 trial ללא כרטיס: start_trial בלי כרטיס/tokenization + next_billing NULL, expire_trials→Free (לא expired), renewal מחייב רק active, תזכורת יום 11 (TRIAL_REMINDER_LEAD_DAYS), migration 022 (trial_ended_to_free), paywall copy. TEST mode. pytest 27/27. | <hash> |
| v0.4.8 | 2026-07-09 | DOCS — יישור מסמכי מקור-אמת ל-ALIGNMENT_2026-07-09: D1 trial ללא כרטיס (change order), D2 Free tier, F13 onboarding simulation, F3 reveal-gating, notes ל-regime_state + episodes, empirical-truth principle. Docs only, אפס קוד/מנוע. | <hash> |
| v0.4.7 | 2026-07-01 | CHORE — pnpm workspace (workspace:* + exports + root lockfile) so @finaroda/scoring-engine resolves natively on Vercel (no toggle). Metadata/config only. | <hash> |
| v0.4.6 | 2026-07-01 | CHORE — frontend → Vercel (vercel.json); resolves the link:../shared monorepo engine that Railway root=frontend couldn't. Backend stays on Railway. | <hash> |
| v0.4.5 | 2026-07-01 | CHORE — frontend build: nix-provided pnpm (nixPkgs) to fix `pnpm: command not found` PATH error. Config only. | <hash> |
| v0.4.4 | 2026-07-01 | CHORE — frontend build: pnpm via npm (not corepack) to fix keyid error; +frontend.Dockerfile fallback. Config only. | <hash> |
| v0.4.3 | 2026-07-01 | CHORE — frontend/nixpacks.toml (Node-only) + packageManager, so Railway frontend builds as Node not Python. Config only. | <hash> |
| v0.4.2 | 2026-07-01 | CHORE — Next.js 15.5.4→15.5.19 (CVE-2025-66478 fix) + eslint-config-next, to unblock Railway staging. Dep bump only. | <hash> |
| v0.4.1 | 2026-07-01 | P2 scorer wired — real scoreDirection (momentum profile) live, 85/82 PASS/WATCH gate on, pullback/continuation logged; score_log.profile (mig 021) | <hash> |
| v0.4.0 | 2026-07-01 | P2 — scan core: client-side Bybit fetch + Trading Blueprint (levels engine imported), Analysis Lens, Risk Style, persistence; score marked pending (pass 2) | <hash> |
| v0.3.1 | 2026-07-01 | DOCS — regulatory reframing (calculator terminology, formula transparency, Analysis Lens, Risk Style, RED LINE). PRD v2.1 · UX v1.2 · LEGAL v2. Engine unchanged. | <hash> |
| v0.3.0 | 2026-07-01 | P1 — תשתית חיה: auth מוקשח (magic-link+Google+beta gate), Cardcom v11 (TEST mode), deploy config, frontend מחובר | <hash> |
| v0.2.1 | 2026-07-01 | P1.5 — placeholder ל-scoring-engine המשותף (shared/), ממתין לחילוץ מהכלי האישי | <hash> |
| v0.2.0 | 2026-06-30 | P0 — שלד נקי: backend+frontend, 18 migrations מודרניות, Cardcom יחיד, validation ירוק | <hash> |
| v0.1.0 | YYYY-MM-DD | Init — scaffolding + מסמכי מקור-אמת ובקרה | <hash> |
