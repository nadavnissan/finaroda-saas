// (auth) — magic-link + Google login, beta gate (SPEC §2.3, §3.1).
// P0 placeholder only. Implemented in P1.
export default function LoginPage() {
  return (
    <main>
      <h1>Sign in</h1>
      <p>Placeholder — auth lands in P1.</p>
    </main>
  );
}
