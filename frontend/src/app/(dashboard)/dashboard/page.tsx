import { DISCLAIMER } from "@/lib/strings";

// (dashboard) — client "what would have happened" board (SPEC §2.3, §8.1).
// P0 placeholder only. Implemented in P3.
export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>Placeholder: &quot;what would have happened&quot; board lands in P3.</p>
      <small>{DISCLAIMER}</small>
    </main>
  );
}
