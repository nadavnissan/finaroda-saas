import { DISCLAIMER } from "@/lib/strings";

// (scan) — FINARODA core: button → scan animation → rings → decision card.
// P0 placeholder only (SPEC §2.3, §6). Implemented in P2.
export default function ScanPage() {
  return (
    <main>
      <h1>Scan</h1>
      <p>Placeholder — scan core lands in P2.</p>
      <small>{DISCLAIMER}</small>
    </main>
  );
}
