// (admin) — admin panel (inherited + adapted; SPEC §2.3, §8.2).
// P0 placeholder only. Implemented in P5.
export default function AdminPage() {
  return (
    <main>
      <h1>Admin</h1>
      <p>Placeholder — admin panel lands in P5.</p>
    </main>
  );
}
