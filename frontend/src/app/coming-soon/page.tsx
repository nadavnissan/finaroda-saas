// coming-soon — pre-launch landing + waitlist capture (SPEC §2.3, §3.1).
// P0 placeholder only.
export default function ComingSoonPage() {
  return (
    <main>
      <h1>Coming soon</h1>
      <p>Placeholder — waitlist capture lands with auth in P1.</p>
    </main>
  );
}
