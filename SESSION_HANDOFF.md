# SESSION HANDOFF — FINARODA SaaS

> מוחלף בסוף כל סשן. מסמך "איך להיכנס", לא ארכיון.

---

> NOTE: from 2026-07-01 this handoff is written in **English** (terminal mangles Hebrew).

## Where we are now
- **Active branch:** dev
- **Last commit (dev):** UPGRADE-STAGE3R (v0.16.0) — PSP migration Cardcom -> **Stripe** (Stripe Checkout + Stripe Billing). Cardcom code fully DELETED. Hosted checkout (card never on our servers), webhook-only activation, Stripe Billing + Smart Retries own recurring/dunning, cancel_at_period_end, our billing state machine survives (fed by webhooks), new Israeli tax-invoice provider layer (MOCK default). Backend + frontend + docs. Runs in DEV/mock until Nadav wires the live Stripe account.
- **Validation:** ✅ **pytest 128/128**, tsc clean, eslint clean (`next lint`), frontend unit **51/51**, shared **14/14**. All Stripe calls mocked, zero network (AC8). No em-dash in new UI copy. New baseline 128 (was 129: net of deleted Cardcom cases + new TC-R3). ⚠ `next build` still NOT verified locally — Windows `.next/trace` EPERM lock / cold-build timeout (environment, not a code error). Recommend a clean `pnpm install` + `pnpm build` on a fresh checkout before the dev→main merge.
- **⚠ New backend dependency:** `stripe>=9,<13` was added to `backend/requirements.txt` and installed into the local `.venv`. On any fresh checkout / deploy run `pip install -r backend/requirements.txt` before the app boots.
- **main:** = `1338a26` (P2 scorer). Everything since (v0.4.2–v0.16.0) is **dev only** — Nadav merges to main manually.
- **NEXT RUN:** "Stage 4 — Coupons + Referral, **redesigned Stripe-native**" (Stripe promotion codes / coupons + a referral model built on Stripe). The dormant `003_coupons` / `004_referral` tables (superseded model) get reshaped or dropped in that run.

## Stage 4 (Coupons + Referral) — HALTED PRE-CODE (2026-07-14)
- **Status:** attempted, **halted before any code was written**. No files changed, no migration added, no commit. Version NOT bumped (stays v0.14.0). Working tree clean of Stage-4 code.
- **Reason:** founder decision (2026-07-14) — the payment provider is switching from **Cardcom to Stripe** (Stripe Checkout + Stripe Billing). Stage 4's coupon + referral mechanics will be redesigned **Stripe-native**, so the original Cardcom-based Stage 4 plan is shelved.
- **Investigation done this run (kept for the redesign):** read the Stage 3 billing spine end to end and resolved the spec's stop points:
  - **S1 (arbitrary discounted first-charge amount) — cleared:** Cardcom's LowProfile/Create already takes an arbitrary integer-agorot `Amount` (`cardcom_service.initiate_checkout`), so a discounted amount could be passed directly (no charge-then-refund). Moot under Stripe (promotion codes handle this natively).
  - **S2 (100%-coupon: active, no token, non-renewing → clean expiry, not dunning) — was TRIGGERED, now VOID:** the current state machine could not express it (ACTIVE→EXPIRED is illegal; `run_renewal_batch` would dun a token-less active user). **Decision on record: no state-machine extension now** — the 100%-coupon path will use **Stripe-native promotion codes** in the redesigned Stage 4. S2 left unanswered by design.
  - **S3 (referral binding vs magic-link) — cleared:** the user row is created at `POST /api/auth/magic-link` on the origin device, so same-device referral binding is safe. Carry into the Stripe redesign.
- **Dead legacy confirmed (leave untouched this run):** migrations **`003_coupons`** (percent-only, no fixed-amount / plan-restriction / first-charge scoping) and **`004_referral`** (50%-off after 3 paid months + admin approval) encode a **superseded model** that contradicts the Stage-4 decisions (D-C1/C4/C5). They are dormant (referenced only by the migrations, `test_smoke` existence asserts, and the `admin.py` `"referrals": 0` placeholder — no live reads/writes). **Decision on record: leave them untouched this run; they will be reshaped or dropped in the redesigned Stage 4 (Stripe) migration.** Documented, no action taken.
- **Untouched (as required):** all code, all migrations, RED LINE §3.5.5, scoring engine/scorer, XP economy, main branch. (Historical note from that halted run; Stage 3R has since shipped the Stripe migration.)

## ⚠️ STRIPE GO-LIVE — what Nadav must do before billing can charge (NONE of this is wired)
The payment layer is fully built on Stripe but runs in DEV/mock (`FEATURE_STRIPE_LIVE=false`, empty `STRIPE_SECRET_KEY`). To go live on the Israeli LTD:
1. **Create the Stripe account (on the LTD, once it is formed):**
   - Register the Stripe account under the LTD; complete business verification (Stripe supports Israel + ILS). Turn on **Stripe Billing**.
   - Get the **API keys** → `STRIPE_SECRET_KEY` (start with the **test** `sk_test_...`, only later the live key), `STRIPE_PUBLISHABLE_KEY`.
2. **Seed the Stripe Products/Prices (test mode first):**
   - `python -m backend.scripts.seed_stripe_prices` (idempotent). It creates one Product + monthly ILS Price per paid plan (basic ₪59, pro ₪149, from `system_settings.plan_price_*`) and stores the Price ids in `system_settings.stripe_price_basic` / `stripe_price_pro`. A price CHANGE = re-run after editing the plan price (creates a NEW Stripe Price; existing subs keep the old one).
3. **Configure the webhook endpoint:**
   - In the Stripe Dashboard add an endpoint → `POST https://<api-host>/api/billing/webhook`, subscribed to: `checkout.session.completed`, `invoice.paid`, `invoice.payment_failed`, `customer.subscription.updated`, `customer.subscription.deleted`.
   - Copy its **signing secret** (`whsec_...`) → `STRIPE_WEBHOOK_SECRET`. (Our verifier implements Stripe's documented `t=,v1=` HMAC-SHA256 scheme over the raw body with a 300s tolerance.)
4. **Choose + wire the Israeli tax-invoice provider (accountant-driven):**
   - Stripe invoices are NOT Israeli tax documents. Pick a provider (Green Invoice / iCount / EZcount) and implement its `create_document(...)` in `core/invoice_provider.py` (the interface + stub are already there), then set `INVOICE_PROVIDER=<name>` + its API credentials on Railway. Until then `INVOICE_PROVIDER=mock` writes an offline MOCK document.
   - Confirm the legal document type with the accountant: default is now `tax_invoice_receipt` (VAT-registered LTD); admin-editable via `system_settings.billing_document_type` (`tax_invoice_receipt` | `invoice_receipt` | `receipt`).
5. **Flip the LIVE switch (Nadav's manual step + lawyer clearance gate):**
   - Set `FEATURE_STRIPE_LIVE=true` and the live `STRIPE_*` keys on Railway **only** after (1)-(4) are done and legal/terms are cleared. Never commit any key.
   - Verify one test-mode end-to-end flow (checkout -> webhook -> active -> tax document -> receipt email) before opening to users.
6. **Railway cron wiring (manual — unchanged endpoint, slimmer job):**
   - Keep the daily `POST https://<api-host>/api/cron/billing` with header `X-Cron-Secret: <CRON_SECRET>`.
   - It now runs ONLY the non-Stripe sweeps: expire card-free trials + drop cancelled-at-period-end subs. There is NO charge step (Stripe Billing + Smart Retries handle recurring + dunning). Idempotent, safe to run twice.
- **Env vars this stage — NEW:** `FEATURE_STRIPE_LIVE` (default false), `STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`, `INVOICE_PROVIDER` (default `mock`), `STRIPE_SUCCESS_PATH` (`/checkout/success`), `STRIPE_CANCEL_PATH` (`/checkout/cancelled`). **REMOVED:** all `CARDCOM_*`, `FEATURE_CARDCOM_LIVE`, `DUNNING_RETRY_OFFSETS_HOURS`. Unchanged + relevant: `BILLING_PERIOD_DAYS` (fallback period date), `CRON_SECRET`.
- **Admin walkthrough (first go-live sanity):** in Stripe test mode, run the seed script, do a test checkout from `/subscribe`, watch `/api/billing/webhook` flip the user to active + issue a MOCK tax document, then cancel from Settings and confirm the end-of-period drop.

## Latest — UPGRADE STAGE 3R: PSP migration Cardcom -> Stripe (v0.16.0, backend + frontend + docs, DB mig 035, env swap)
- **What shipped:** the entire payment layer moved from Cardcom to **Stripe (Checkout + Billing)**, PSP-agnostic. (A) **Checkout** `core/stripe_service.initiate_checkout` creates a hosted Stripe Checkout Session (mode=subscription; the card never touches our servers). DEV fallback (no live key) returns a zero-network fake session that lands on the success page. Activation happens ONLY via the webhook, never the redirect. (B) **Webhook** `handle_webhook` verifies Stripe's `t=,v1=` HMAC signature (hand-rolled so the verify boundary needs no SDK and is testable offline), dedupes by event id (`processed_webhook_events`), and dispatches `checkout.session.completed` (activate + store `stripe_customer_id`/`stripe_subscription_id` + first tax document), `invoice.paid` (recurring document + receipt; subscription_create is documented at checkout), `invoice.payment_failed` (past_due + our dunning email + bell), `customer.subscription.updated` (cancel_at_period_end -> cancelled; recovery/past_due sync), `customer.subscription.deleted` (voluntary -> none, involuntary -> expired). (C) **Recurring + dunning = Stripe Billing / Smart Retries** — the homegrown `run_renewal_batch` + `+24h/+72h` scheduler was DELETED. Our failure/recovery/cancel emails still fire from the webhook (copy stays ours). (D) **Cancel** `cancel_subscription` sets Stripe `cancel_at_period_end` + our `cancelled` state (access to period end) + churn hook; double-cancel safe. (E) **Trial stays ours** — 14-day, card-free, NOT a Stripe trial; expired by the cron. (F) **Tax invoices** `core/invoice_provider.py` — provider-agnostic, MOCK default (offline), documented interface for Green Invoice / iCount / EZcount (`INVOICE_PROVIDER`); doc type default `tax_invoice_receipt` (VAT LTD). Stripe invoices are never Israeli tax documents. (G) **State machine survives** (`core/billing_state.py`) as the entitlement source of truth, fed only by webhooks; one edge added (active -> expired). (H) Frontend `/api/cardcom/*` -> `/api/billing/*`; checkout pages relabeled Stripe.
- **STOP-point outcomes (none halted, all reported):**
  - **S1 (ILS unsupported) — cleared:** Stripe fully supports ILS (Checkout, Prices, Subscriptions, Invoices).
  - **S2 (status mapping needs a new/removed state) — cleared, no state change:** our six states cover every Stripe status we consume; added ONE matrix edge (active -> expired) for an involuntary `customer.subscription.deleted` that never passed through past_due (AC6 sanctions matrix updates). Full mapping documented in `billing_state.py` + `stripe_service.py`.
  - **S3 (deleting dunning breaks a Stage-5 test) — cleared:** `test_stage5_notifications` imports only `trial_ending_soon_task`; failure/recovery emails moved to the webhook, user-facing behavior preserved.
- **DB (mig 035, additive/non-destructive renames):** `users.cardcom_token -> stripe_customer_id` (+ `stripe_subscription_id`, `card_last4`, `card_expiry`); `payment_transactions.cardcom_tx_id -> stripe_reference`, `cardcom_response_json -> provider_response_json`; `billing_documents.cardcom_document_id -> provider_document_id`; new `processed_webhook_events` (event-id idempotency); `billing_document_type` default `receipt -> tax_invoice_receipt`. Money stays agorot ints.
- **Deleted (no dead code):** `core/cardcom_service.py`, `core/cardcom_invoice.py`, `models/cardcom.py`, `api/cardcom.py`, `scripts/run_subscription_renewal.py`, `tests/test_stage3_billing.py`, the homegrown renewal/dunning + the cron charge step, all `CARDCOM_*` env + `FEATURE_CARDCOM_LIVE` + `DUNNING_RETRY_OFFSETS_HOURS`.
- **Decisions this run:** (1) webhook verified by a hand-rolled HMAC (Stripe's documented scheme) so the verify boundary is SDK-free + testable offline; the official SDK is used for outbound Checkout/Subscription/Price calls (D-R1). (2) `subscription.deleted` -> `none` for a voluntary local cancel, `expired` for involuntary; `drop_cancelled_to_free` cron stays as a safety net for cancelled trials (no Stripe sub). (3) doc-type default flipped to `tax_invoice_receipt` (VAT LTD). (4) seed script seeds basic+pro (the shipped paid plans, mig 029); **D-R3 wording "pro / advanced-dormant" reflects an older plan set — advanced was retired in mig 029, so I seeded basic+pro. Reported, not blocking.**
- **⬜ Follow-ups / not done:** the whole live loop is untested against a real Stripe account (none exists yet — see the GO-LIVE box); the Israeli tax-invoice provider is MOCK until Nadav picks + credentials one; a real browser click-through of `/subscribe` -> Stripe hosted checkout -> success/cancel and the webhook-driven BillingBanner states (TC-R3-13 manual); `next build` still unverified locally (Windows lock).
- **Ready for production?** Feature-complete on dev and fully green, but **billing cannot charge until Nadav completes the STRIPE GO-LIVE box** (LTD Stripe account + keys + seeded prices + webhook endpoint + a chosen invoice provider + `FEATURE_STRIPE_LIVE=true` + lawyer clearance). No blocker to the code merge itself. Nadav decides the dev->main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, XP economy, reveal-gating + scan-limit plan semantics, main branch. The `coupon_code`/`referral_source` columns stay inert (Stage 4).

## Latest — UPGRADE STAGE 3: Live Billing (Cardcom) (v0.14.0, backend + frontend + docs, DB mig 034, +2 env)
- **What shipped:** the full billing loop against Cardcom **sandbox/mock**, production-ready but NOT live. (A) **State machine** `core/billing_state.py` — the one server-authoritative source for `subscription_status` (none/trial/active/past_due/cancelled/expired); a legal-transition matrix that raises `IllegalTransition` on an illegal move; `effective_tier` derives entitlements from state (past_due/cancelled keep access, expired/none → free); every status change funnels through `apply_transition` (guard + `subscription_events` audit). (B) **Billing documents** `core/cardcom_invoice.py` — a receipt/invoice-receipt per successful charge, type from `system_settings.billing_document_type` (accountant-driven), table `billing_documents` (mig 034), idempotent per transaction; offline it writes a clearly-marked MOCK doc with zero network, live it calls Cardcom's `Documents/Create` (gated). (C) **Checkout** — the webhook verifies HMAC then activates server-side only (redirect alone never activates), issues the doc, emails a receipt; idempotent by deal id. (D) **Recurring + dunning** — `run_renewal_batch` charges active-due + past_due-retry-due; a failed charge walks the ladder past_due → retry at +24h → retry at +72h → expired → Free, emailing + belling each failure; success recovers past_due → active; idempotent double-run. (E) **Cancel** — end-of-period (status → cancelled, access kept until the paid-through date, cron drops to Free), the existing churn survey now fires the real cancel; canceling twice is safe. (F) **Cron** `POST /api/cron/billing` (X-Cron-Secret). Frontend: `BillingBanner` (past_due/cancelled/expired) on scan + settings, a real cancel dialog, ₪ prices + a "prices include VAT" note.
- **STOP-point outcomes (none halted the build; all reported — see the GO-LIVE box above):**
  - **S1 (sandbox credentials) — triggered:** no `CARDCOM_*` credentials exist in env (placeholders only). Per S1, everything is built against a zero-network MOCK (all gated by `FEATURE_CARDCOM_LIVE=false`), tests stay green, and the exact list of what Nadav must obtain from Cardcom is in the GO-LIVE box. **No live terminal is wired** (AC8: asserted by test — `FEATURE_CARDCOM_LIVE=false`, empty terminal id, `.env.example` unchanged).
  - **S2 (state machine conflict) — none:** the existing `users.subscription_status` CHECK (mig 001) already allows `none/trial/active/cancelled/past_due/expired`, which covers D-B4 one-to-one; only the spelling differs (D-B4 "trialing" → `trial`, "canceled" → `cancelled`). The state machine standardizes on the existing DB vocabulary — mapping documented, **no rename, no value migration**. Entitlements: breadth still derives from `tier`; the STATE decides whether the tier is paid at all. No conflict with reveal-gating or scan limits.
  - **S3 (invoice module business settings) — CONFIG + questions listed:** with no Cardcom account, the live document path is scaffolded + gated, and the document type is CONFIG (`billing_document_type`, default `receipt`). The exact accountant questions are in the GO-LIVE box. Offline = MOCK doc, never a real fiscal document.
- **Schema (mig 034, additive/non-destructive):** new `billing_documents` (per-charge doc, agorot, unique per transaction, + inert `coupon_code`/`referral_source`); `payment_transactions += coupon_code, referral_source (D-B7 inert), kind ('first'|'recurring')`; `users += dunning_next_retry_at` (`billing_failure_count` from mig 019 stays the attempt counter); `subscription_events` CHECK rebuilt (mig 022 pattern) to allow 5 new event types (`payment_document_issued`, `subscription_past_due`, `dunning_retry_scheduled`, `subscription_expired_dunning`, `subscription_dropped_to_free`); seed `billing_document_type=receipt`.
- **Decisions this run:** (1) state machine on the existing DB vocabulary (S2) — no rename churn on 112 passing tests. (2) receipt + dunning emails respect the Stage-5 `email_product` pref. (3) dunning cadence is config (`DUNNING_RETRY_OFFSETS_HOURS=24,48` ⇒ retries at +24h and +72h from the first failure; 2 retries then expire). (4) `billing_failure_count` reused as the attempt counter; a past_due→past_due reschedule is a plain `dunning_retry_scheduled` audit row, not a state transition. (5) offline documents are MOCK, clearly marked.
- **⬜ Follow-ups / not done (browser + go-live):** a real browser click-through at 390px & 1280px of the past_due/cancelled/expired `BillingBanner`, the Settings cancel dialog (cancel + survey + access-until), and the Subscribe VAT note (TC-B3-10 manual); the whole live loop is untested against a real terminal (no account) — S1/S3 items above; `next build` still unverified locally (Windows lock). Coupons + referral logic = Stage 4 (columns exist, inert).
- **Ready for production?** Feature-complete on dev and fully green, but **billing cannot charge until Nadav completes the GO-LIVE box** (Cardcom credentials + invoice module + accountant document type + lawyer clearance + `FEATURE_CARDCOM_LIVE=true` + Railway cron). No blocker to the code merge itself. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, XP economy, reveal-gating + scan-limit plan semantics, main branch. No real terminal wired.

## Latest — UPGRADE STAGE 6: Academy 2.0 (v0.13.0, backend + frontend + docs, DB mig 033, no new env)
- **What shipped:** the Academy rebuilt end to end. (A) **Card grid** replaces the list (`academy/page.tsx`) — each card shows title, short description, duration, type badge (text/video), lock state, completion state; responsive (1 column at 390px, multi-column at 1280px via `grid auto-fill` inside a `maxWidth:1040` column). (B) **Search + filters** client-side (`lib/app/academy.ts`: `filterLessons` over title/description/tags × type × locked/unlocked/completed, combined AND, instant). (C) **Video lessons** — `content_type='video'` + a YouTube/Vimeo URL (no uploads); `VideoEmbed.tsx` lazy-loads the iframe only on tap (YouTube gets a thumbnail poster). (D) **Admin management** (`api/admin_academy.py` + `AcademyAdmin.tsx`, new admin "academy" section) — create/edit/reorder/archive/restore, video attach, tag editing, dual-gate assignment; admin-only (403), every mutation audited to `admin_events`.
- **Engine change:** the B6 hardcoded `_MODULES` list became a DB table **`academy_lessons` (mig 033)**: slug (completion key), title, description, content_type, body, video_url, duration_minutes, tags(JSON), **min_plan(free/basic/pro) + min_rank(0/1000/3000/8000)**, sort_index, awards_xp, archived_at. Gating logic lives in `core/academy_gate.py`.
- **STOP-point outcomes (none halted, all reported):** **S1 (structure conflict) — cleared:** the shipped academy (B6 / PRD F6) is already flat (12 modules), which a card grid matches. The mig-009 `academy_bundles`/`academy_episodes` VOD tables (a two-level bundle→episode tree from the template) exist but are **dormant** — no live code reads/writes them (only `test_smoke` asserts they exist). Dead legacy, not "the current list", so no conflict with a flat grid; superseded at the product level, tables left untouched. **S2 (plan ambiguity) — cleared:** code CHECK is `free/basic/advanced/pro` but `advanced` is dormant (mig 029 migrated it to basic, tolerated in the CHECK). Three active tiers match the docs; min_plan ∈ {free,basic,pro}; legacy `advanced` treated as paid (>=basic); `trial` = Pro access. No genuine ambiguity. **S3 (migration risk) — cleared, this was the real one:** completions live entirely in `xp_events` (source=`academy_lesson`, ref=`module_id`). Mig 033 is **purely additive** (creates the table, touches zero `xp_events` rows) and seeds the 12 lessons with `slug == old module_id`, so every completion + its +100 XP survives keyed by the same slug. Count-match asserted (TC-AC6-02).
- **D-AC5 reorder mechanism chosen:** **up/down buttons** (not drag). Each ▲/▼ swaps the row with its neighbor and POSTs the full new order to `POST /api/admin/academy/lessons/reorder` (`ordered_ids`), which rewrites `sort_index` by position. Chosen over drag for zero-dep reliability on this inline-styled, no-DnD-library codebase.
- **Migration summary (mig 033):** creates `academy_lessons` + a `(sort_index,id)` index, seeds the 12 B6 modules (slug==old id; title/duration preserved; B6 tier `basic`→min_plan `free`, `full`→`basic`; rank_unlock None/1000/3000→min_rank; the 3 stubs `volume_basics`/`positioning_basics`/`regime_transitions`→`awards_xp=0` so their +0/completed=false parity is byte-identical to B6). `body` is seeded from `concept_tooltips_content.json` (the same text B6 rendered client-side, now stored + server-gated) via a guarded read that falls back to empty if the file is absent. **No user loses a completion or any XP.**
- **Seed structure ready for Nadav's content:** the 12 lessons ship as `content_type='text'` with their existing body. To add real content Nadav uses the admin "academy" section: edit a lesson → switch to VIDEO → paste a YouTube-unlisted/Vimeo URL (validated + normalized to an embed URL; a bad URL is rejected with a clear error), or edit the text body, tags, duration, min_plan, min_rank, and the "awards +100 XP" flag. New lessons via "+ NEW LESSON". Reorder with ▲/▼, hide with ARCHIVE (restorable, never deletes; granted XP is never revoked).
- **AC9 — em-dash lint finding reported IN FULL (the Stage-7 report was truncated "…uct copy"):** the guard is `test_no_em_dash_in_product_copy` in **`backend/tests/test_content_copy.py:33-46`**. It rglobs `frontend/src/**/*.{ts,tsx}`, skips a line only when its lstripped text starts with `*`, `//`, or `/*` (comment lines) and drops trailing `//` comments, then flags any remaining U+2014. **The gap:** a JSX expression-container comment `{/* … */}` starts with `{` after lstrip, so it is NOT recognized as a comment — an em dash inside one would be flagged as "product copy" even though it is a comment and **not user-facing**. It is a pre-existing lint limitation (a false-positive class), not a code bug; I did **not** modify the test (out of scope, and it is Nadav's copy-guard) and kept all new Academy code clear of em dashes anywhere, including `{/* */}` comments. No new em-dash violations exist (pytest green).
- **Decisions this run:** (1) `min_rank` stored as the XP floor int (0/1000/3000/8000), directly comparable to `xp_total`. (2) per-lesson `awards_xp` boolean preserves B6 stub behavior (3 seeds = 0 XP) so every existing test stays green without editing it. (3) locked content returns **403 + plain-language reason** (not 404) from `GET /api/academy/{slug}` — the list still shows the locked card's metadata (D-AC7 "show the door, name the key"). (4) list response keeps the key `modules` and per-item aliases `id`/`minutes` for backward compat with the B6 client + existing tests.
- **⬜ Follow-ups / not done:** a real browser click-through at 390px & 1280px of the card grid, search/filters, the lazy video player, a locked card's reason, and the admin create/edit/reorder/archive flow (TC-AC6-14 manual); **lesson content itself is Nadav's task** (12 seeds carry their B6 text; video URLs + richer bodies are his to add via admin); `next build` still unverified locally (Windows lock).
- **Ready for production?** Green on dev, all ACs pass. No blocker to the merge itself; recommend a clean `pnpm install` + `pnpm build` and the manual click-through first. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, **XP economy (no rule change — same +100 once-per-lesson `academy_lesson` source; rank gating is STATUS, never XP-as-currency)**, `xp_events` rows, main branch. Dormant mig-009 VOD tables left as-is.

## Latest — UPGRADE STAGE 7: Admin v1.1 + Sentry + Ticket Breadcrumbs (v0.12.0, backend + frontend + docs, DB mig 032, env vars added)
- **What shipped:** (A) **Admin user table v1.1** — new columns (scans total/week, active-days 7d/30d, XP, rank, referrals placeholder 0, churn flag), server-side AND filters (plan / status trial|active|expired|churned / signup-range / last-active-range / min_scans) with URL-encoded state (shareable + refreshable), and a **CSV export** of the current filtered view (admin-only, row-capped). (B) **Sentry** backend + frontend, env-gated, PII-scrubbed. (C) **Breadcrumbs** — last 20 client events attached to every support ticket, rendered as a timeline in the admin ticket view.
- **STOP-point outcomes:** **S1 (admin spec conflict) — none:** SPEC F9/B7 defines churn (rate + exit-survey reason), tickets, and MRR; D-A2/D-A3 only add detail (XP/scans/active-days) without contradicting it. No halt. **S2 (breadcrumbs vs reveal-gating) — safe:** breadcrumbs pass through a server-side allowlist sanitizer (`core/breadcrumbs.py`) that drops every non-whitelisted field, so no journal outcome value (status/r_result) can be stored; the client never holds unrevealed values either. Asserted in tests. No halt.
- **D-A5 churn-survey hook (reported):** there is NO cancel/downgrade/account page in the app (Stage 3 payments is blocked, and no such UI existed). I wired the survey to a **new, payment-decoupled endpoint `POST /api/churn/survey`**, surfaced from **Settings → "Cancel plan or leave"** (skippable, one required question + optional free text). It stores to `churn_reasons` (mig 006, already existed) and does NOT perform any billing action. When the Stage-3 cancel flow ships, call this same endpoint from that confirmation step.
- **D-A8 ticket case (reported):** a ticket system already existed (`support_tickets` mig 018, `api/support.py`, admin queue/reply in `api/admin.py`, `ticket_replies` mig 028). I **extended** it — added `support_tickets.breadcrumbs` (mig 032) + sanitize-on-write + admin timeline render. Did not rebuild.
- **Env vars (NEW):** `SENTRY_DSN` (backend; `SENTRY_DSN_BACKEND` still works and takes precedence), `NEXT_PUBLIC_SENTRY_DSN` (frontend). All absent → Sentry fully disabled, zero network. Sample rate traces 0.1. Add these on Railway (backend) and the frontend host only when you want error reporting live.
- **⚠ Frontend Sentry activation (manual):** `@sentry/nextjs` is declared in `frontend/package.json` but loaded via a runtime dynamic import (so tsc/lint/build never hard-depend on it). To actually activate on a deploy: `pnpm install` (pulls @sentry/nextjs), set `NEXT_PUBLIC_SENTRY_DSN`, and `pnpm build`. `src/instrumentation.ts` (server) + `src/instrumentation-client.ts` (client) call the env-gated init. Not validated locally (same Windows `next build` limitation as prior stages).
- **Railway notes:** no new cron. Backend Sentry auto-inits on startup when `SENTRY_DSN`/`SENTRY_DSN_BACKEND` is set (already wired in `main.py`).
- **Decisions this run:** (1) `notifications_log`-style separation kept: `active-days` is admin-analytics only (D-A1) — read-only from scan_events, never user-facing (a `/api/profile` test asserts it never leaks), never grants XP, never gates. (2) `status=churned` = the user submitted an exit survey (distinct from a silently-expired trial). (3) Frontend Sentry via dynamic import to keep the build green without the package installed.
- **DRIFT FOUND this run (reported, not silently fixed):** the em-dash lint (`test_no_em_dash_in_product_copy`) also flags JSX block comments `{/* ... */}` as product copy (they do not start with `//`/`*`/`/*` after lstrip). Non-intuitive but pre-existing behavior; I kept the new code clear of it. No other new drift.
- **Drift-fix applied (authorized):** removed the em-dashes from the welcome + beta-approved email templates (`core/email.py`) that were reported in Stage 5.
- **⬜ Follow-ups / not done:** browser click-through of the admin filters/CSV/ticket-breadcrumbs at 390px & 1280px (TC-A7-14 manual); live Sentry activation (install + DSN + build on CI); ticket-reply email is still a logged stub; referral count is a hardcoded 0 until Stage 4.
- **Ready for production?** Green on dev. Sentry is inert until DSNs are set and (frontend) the package is installed on a build box. No blocker to the merge itself, but do a `pnpm install` + `pnpm build` on a clean checkout first. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, XP economy (no XP rule change — rank/active-days are display/analytics only), main branch.

## Latest — UPGRADE STAGE 5: Notifications + Bell + Preferences + Real Resend (v0.11.0, backend + frontend + docs, DB mig 031, +1 env var)
- **What shipped:** a full notification system. **Bell** in the hamburger (`NotificationBell.tsx` inside `NavDrawer`): server-authoritative unread badge (capped "9+"), newest-first panel, marks the visible items read on open (survives refresh). **Prefs** (`notification_prefs`, cross-device): in-app / sound / vibration / product-email / broadcast-email, toggled from Settings. **Real Resend** (`core/email.py`): pure renderers + `_send_or_log` that posts to Resend when `RESEND_API_KEY` is set, else logs the full payload (DEV fallback, mirrors `DEV_RETURN_MAGIC_LINK`). **Three flows:** day-11 trial reminder, reveal-teaser (zero outcome values — pull-only red line), admin broadcast (opted-in only + mandatory signed unsubscribe).
- **Data (mig 031):** `notifications` (bell feed: id/user_id/type/title/body/link_path/created_at/read_at, index on (user_id,read_at)); `notification_prefs` (PK user_id, 5 flags default true); `journal_scenarios.teaser_sent_at` (per-reveal teaser dedup flag). `notifications_log` (mig 028) is unchanged — it stays the idempotency + admin-audit ledger, distinct from the user-facing feed.
- **API:** `GET/POST /api/notifications` (+ `/read`, `/prefs`); `GET /api/email/unsubscribe?token=` (no login, HMAC over JWT_SECRET, idempotent, plain HTML page); `POST /api/cron/notifications` (header `X-Cron-Secret`, idempotent); admin `GET /api/admin/broadcasts/preview` + enhanced `POST /api/admin/broadcasts` (real email fan-out to opted-in recipients, in-app bell rows, delivered counts).
- **STOP-point outcomes:** **S1 (trial length) — cleared, no halt:** SPEC §5 "Trial 14 יום", seed `trial_reminder_day=11`, config `TRIAL_DAYS=14`/`TRIAL_REMINDER_LEAD_DAYS=3` → day-11 = 3 days left, exactly as assumed. **S2 (sending domain) — reported, non-blocking:** `EMAIL_FROM_BRAND` already defaults to `FINARODA <noreply@finaroda.com>`; I did NOT invent a domain. All build/test runs use the DEV console-fallback (no `RESEND_API_KEY`), so no verified domain is needed to develop or test. **Before any live send, finaroda.com must be verified in Resend** (OPEN_ITEMS: domain decided, buy+connect at hardening / Stage 8). Until then, set `RESEND_API_KEY` only after the domain is verified, or live sends will bounce.
- **⚠ Railway cron wiring (MANUAL — Nadav):** the scheduled sweeps are exposed as an HTTP endpoint, not auto-scheduled. Add a Railway Cron (or scheduled job / external scheduler) that runs, e.g. daily:
  - `POST https://<api-host>/api/cron/notifications` with header `X-Cron-Secret: <CRON_SECRET>`.
  - Example: `curl -fsS -X POST "$API_URL/api/cron/notifications" -H "X-Cron-Secret: $CRON_SECRET"`.
  - Idempotent (safe to run twice; a second run in-window sends zero). The old `python -m backend.scripts.run_resolve_scenarios` now ONLY resolves market outcomes; the day-11 + reveal-teaser SENDS moved to this endpoint (so run resolve-scenarios first, then the notifications cron).
- **Env vars:** **new — `CRON_SECRET`** (required for the cron endpoint; empty ⇒ endpoint returns 503, fail-closed). Already-existing, relevant to go-live: `RESEND_API_KEY` (empty ⇒ DEV fallback), `EMAIL_FROM_BRAND` (sender, default `FINARODA <noreply@finaroda.com>`), `EMAIL_REPLY_TO`, `EMAIL_SENDING_ENABLED`. Add `CRON_SECRET` to `.env.example` / Railway before wiring the cron.
- **Decisions made this run:** (1) `notifications` feed and `notifications_log` ledger coexist as separate concerns. (2) reveal-teaser uses a sent-flag on the reveal row (`teaser_sent_at`) and collapses a user's multiple pending reveals into ONE teaser per sweep (honors "one teaser per pending reveal" without spamming). (3) `log_trial_reminders_task` is DEPRECATED and de-wired; `trial_ending_soon_task` via the cron endpoint is the single authoritative day-11 path.
- **DRIFT FOUND this run (reported per CLAUDE.md, not silently fixed elsewhere):** (a) mig 005 created a `notifications` scheduled-outbox table that NO application code reads or writes; it collided with the D-N1 bell-feed name, so mig 031 RENAMES it to `notifications_legacy_outbox` (non-destructive, preserves any rows) — flagged for Nadav to drop later if truly dead. (b) The pre-existing welcome + beta-approved emails (`core/email.py` lines ~106/121/127) contain em-dashes in user-facing copy (D-N10) — NOT authored or touched by this task; left as-is for Nadav's call. (c) Two day-11 code paths existed with divergent idempotency refs (billing vs journal task) — consolidated onto the billing task.
- **⬜ Follow-ups / not done:** live email send requires the verified domain (above); a real browser click-through of the bell open/mark-read, the 5 Settings toggles, and the admin broadcast preview+confirm at 390px & 1280px; arrival sound/vibration verified only via unit logic (no live-browser check); Railway cron not yet wired.
- **Ready for production?** Feature-complete on dev and fully green, but **NOT** ready to send live email until finaroda.com is verified in Resend and `CRON_SECRET` + the Railway cron are wired. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, XP economy (no XP change), main branch.

## Latest — RESPONSIVE PASS: phone (~390px) + desktop usability (v0.10.1, frontend + docs, no DB/config)
- **What shipped:** every route usable on a phone and on desktop, with a permanent regression guard. **Key finding:** the product screens were already mobile-first from Package B / FIX-R3R4 (each centers a `maxWidth` column; charts are SVG viewBox + `width:100%`; tooltips clamp) — so the ONE real offender was the **admin console** (desktop-first: fixed 200px rail + 280/340/300px side panels + dense 5-col grids), which overflowed a phone.
- **`useIsMobile` hook** (`frontend/src/lib/app/useIsMobile.ts`): `matchMedia`, mounted-guarded (returns desktop until client mount → no hydration mismatch). Inline-styled components can't do CSS media-queries, so layout-swapping screens branch on this.
- **globals.css safety layer:** `body { overflow-x: hidden }` (never a horizontal page scrollbar — wide tables scroll in their own container), `main` padding → `clamp(0.9rem, 4vw, 2rem)` (was a fixed 2rem = 64px of a 360px phone; now ~14px on a phone, unchanged on desktop), `img/svg/video/canvas/table/pre { max-width: 100% }`.
- **Admin responsive** (`app/(admin)/admin/page.tsx`): the admin `<main>` explicitly overrides the global `main` rule (which leaks `flex-direction:column + align-items:center + 2rem padding + text-align:center` into every `<main>`) so it lays out edge-to-edge. Rail → **sticky top bar + horizontally-scrollable tab strip** on mobile / 200px rail on desktop. Overview grid → `repeat(auto-fit, minmax(140px,1fr))` (5→2). Users & Tickets → **master/detail swap** on mobile (selecting an item replaces the list, with a "← back", instead of a fixed side panel that overflows); Users list → stacked cards on mobile. Broadcast → stacks. Notifications log → `overflowX:auto` scroller with a **sticky first column** (never clipped). Settings locked cards → column. Desktop is unchanged (B7 full-width frames).
- **Copy (the one allowed change):** dashboard journal reveal caption is now tier-aware — Free (1 scan/day) reads **"Revealed on tomorrow's scan, never by push."**, paid/trial keep **"…on your next scan…"**.
- **Regression harness (Nadav's decision: structural lint, not Playwright):** `frontend/tests/viewport.regression.test.ts` runs in the existing `node --test` with zero new deps. Rationale documented in the file: a real `scrollWidth<=clientWidth` needs a browser (jsdom = no layout; Playwright/Next-server unreliable on this Windows box where builds time out — a gate that fails for env reasons gets ignored). Nadav's manual phone validations are the real-browser measurement; the lint guards the specific regression patterns between them. **Extensible by design** — `PATTERNS` / `MAXWIDTH_ROUTES` / `SCROLLER_FILES` are commented const arrays; encode every future manually-found overflow as a new entry. Checks: no fixed `width ≥360` without a maxWidth/vw/mobile guard; every product route has a `maxWidth` column; admin uses `useIsMobile` + a non-centered main; wide tables sit in an `overflowX` scroller; chart is `width="100%"`+viewBox; globals has the guards.
- **⬜ Follow-ups / not done:** a real **browser click-through at 390px & 1280px of every route** (esp. admin tickets/broadcast/notifications, S11 table, the scan Blueprint modal) — the lint is structural, not a live pixel measurement (TC-RESP-01 is manual). **Playwright E2E is a candidate for CI later**, when the pipeline runs on a machine where `next build` completes. `next build` still unverified locally (Windows lock).
- **Ready for production?** Not yet — recommend Nadav's phone validation of the admin + the manual TC-RESP-01 sweep first. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch. No backend/DB/config change. Product screens that were already responsive were left as-is (no gratuitous edits).

## Latest — FIX-R3R4: validations 3+4 fix round (v0.10.0, code + migrations 029/030)
- **What shipped:** the 11 bugs + 5 approved decisions (A–E) + the UX additions from Nadav's validation-3/4 click-throughs. Backend money/gating first (committed `e2e49b8`), then the frontend (committed `b852dd7`), then docs.
- **Decision A — three plans (Advanced retired):** Free/Basic/Pro, coins 2/5/10, prices Basic ₪59 / Pro ₪149 marked **PENDING-ACCOUNTANT** in docs. `mig 029` migrates any `advanced` user → `basic` (same 5-coin breadth) and retunes/prunes `system_settings` (scan_coins_basic=5, plan_price_basic=5900, plan_price_pro=14900 agorot; advanced_* keys deleted). `/api/plans`, entitlements defaults, cardcom `VALID_PLANS`, model Literals, admin editable/plan-override, and academy full-access (basic+pro) all updated. **The `users.tier` CHECK was NOT rebuilt** — `advanced` is tolerated as a legacy value (non-destructive, reversible).
- **Bug 3 — Free 1-scan/day now ENFORCED:** `POST /api/scan/events` returns **429 `DAILY_SCAN_LIMIT`** past the cap (counts today's `scan_events`); Free=1/day, paid=unlimited; admin-editable via `scans_per_day_*`. Client shows a friendly "limit" state (SEE PLANS / VIEW JOURNAL). NB: this changed the journal-reveal tests (they scan twice/day) — 3 existing tests were moved to an unlimited tier.
- **Bug 4 — trial to /scan:** onboarding S11 "Start 14 days" and the Subscribe trial CTA now call `POST /api/cardcom/trial` directly and land on `/scan` with a **TRIAL** header chip (no tokenizing screen). Day-11 reminder writes an idempotent `notifications_log` row (email_in_app). Expiry→Free unchanged.
- **Bug 2 — XP at S9:** the 300 grant + call-sign persist + completion moved to **S9** (call-sign submit); S11 keeps an idempotent `complete()` safety-net, so a broken exit path never costs the grant.
- **Bug 1 — flash:** `runScan` now holds the scanning overlay until the async reveal resolves (S2/S8/S10), so no stale pre-scan frame renders between SCAN and reveal. **S5→S6 flash:** no scan/redirect exists in that transition in code (page wrapper is a plain render, mount routing-check runs once) — could not reproduce by code analysis; **needs a browser confirm as a fresh incognito user.**
- **Bug 5 / 6 / 7 / 8:** SEE PLANS round-trip restores the RESULTS state (sessionStorage, coins still tappable); S8 placeholders consistent (E3 risk 0.1511 is seeded); S1 title deliberate 2-line split; SCAN button reads "N COINS".
- **Decisions B/C/D/E:** Recent-scans history page (`GET /api/scan/history[/{id}]`, read-only) + drawer item; coin picker within the plan count (localStorage, enforced in the scan); paid why-not "THE ACTUAL NUMBERS" (price vs EMA200 %, EMA7 slope, volume); Lens=Full / Style=Balanced marked default.
- **UX:** logo→/scan; call-sign greeting (email fallback) on the dashboard; "How is R measured?" explainer (r_multiple tooltip + caption); Profile vs Settings split; login B6a + **DEV SIGN-IN** (surfaces `dev_magic_link`); admin "Back to app" + notifications explainer; tickets attach `app_version` (`mig 030`) and the admin ticket view returns the reporter's last 20 logged events. `.env.example`: `DEV_RETURN_MAGIC_LINK=true` with a production-guard note.
- **⚠ Decisions I made (need Nadav sign-off):** (1) legacy `advanced` tolerated in the tier CHECK + migrated to `basic` rather than a destructive table rebuild; (2) **Basic inherits Advanced's full academy** (not just chart/coins); (3) enforcing scans/day means a Free user's journal reveal happens on the *next day's* scan (the reveal needs a 2nd scan) — the 3 reveal tests were bumped to unlimited tiers to keep exercising the mechanic; (4) XP grant moved earlier to S9 per the "broken exit must not cost the grant" intent.
- **⬜ Follow-ups / not done:** `next build` local verification (Windows lock); a real browser click-through of the whole flow (esp. the S5→S6 flash, the coin picker, the trial chip, the daily-limit state); an automated test for the reveal-gap (hard without jsdom — covered by manual ATP TC-A-105); the Subscribe paid-checkout `redirect_url` contract is assumed (degrades gracefully in 503 test mode).
- **Ready for production?** Not yet — recommend the browser click-through + a clean `pnpm build` first. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## DOC DRIFT (predates current task — for Nadav's call, NOT silently fixed)
> Per CLAUDE.md §2.x: divergences discovered during the DOCS-SYNC run that predate it (introduced by the Package B / F13 commits, outside this task's named PRD/SPEC/UX scope) are listed here rather than edited. `FINARODA_ONBOARDING_SPEC.md` is the affected source-of-truth doc; it was not in the DOCS-SYNC task's scope. Decide whether to reconcile it or leave the drift.
- **`FINARODA_ONBOARDING_SPEC.md` line 40 — E1 = LINK (stale).** Says "E1 trap = LINK 30/06→02/07". Shipped E1 was **re-picked to BTCUSDT** (LINK 30/06→02/07 actually rose in real klines — a curation error; see `EPISODES_AND_VERIFIED_NUMBERS.md`). Line 50 of the same doc **already** reflects BTC — so the doc is internally contradictory and line 40 is the stale one.
- **`FINARODA_ONBOARDING_SPEC.md` line 109 — `computeRangeLevels` (removed).** Chart Standard v1 is described with "swing S/R (`computeRangeLevels`)". That fn was **removed** in v0.8.0; the canonical S/R is now `findRecentSwingLevels` via the `swingLevels` adapter (equivalence-tested). Reconciled in SPEC §6.1 this run; the onboarding spec still names the old fn.
- **`FINARODA_ONBOARDING_SPEC.md` lines 118 & 125 — recharts (superseded).** The Design-components/dependencies rows say the episode chart renders via "recharts". Shipped rendering is **in-app SVG** (EpisodeChart / Chart Standard v1) — line 109 of the same doc already says "in-app (SVG)", so the doc is internally inconsistent. (Same recharts-vs-SVG open decision noted for PRD/SPEC/UX; recharts remains a listed dep.)
- **Checked & in sync:** `XP_ECONOMY.md` v1.0 matches shipped code (4 user-earnable sources + audited `admin_grant`, ranks 1000/3000/8000, server-side idempotent) — no drift.

## Latest — DOCS-SYNC: source-of-truth docs reconciled with Package B (v0.9.1, docs only, no code)
- Aligned **PRD/SPEC/UX** to the implemented state after Package B phases 1+2 (v0.8.x–v0.9.0). **No app/engine/scorer/DB change.**
- **PRD F3 = the shipped `journal_scenarios` model:** documented the `pass` / `no_setups_day` records (WATCH is never a scenario), server-side resolution + status enum, and reveal-gating. **CAPITAL SAVES defined** = a PASS whose trigger never filled in the window (capital preserved, r=0); a **note** marks the per-coin non-passer SAVE row as a **future F3 extension** (not v0.9.0, needs Nadav's call).
- **SPEC data model += §5.9 (new):** full `journal_scenarios` DDL (mig 028) + `ticket_replies` / `notifications_log` / `user_settings`; `admin_events` (mig 006) documented as B7's audit trail; endpoints table (`GET /api/scan/entitlements`, `GET /api/plans`, journal/profile/support/broadcasts); `system_settings` keys (`scan_coins_free`, `chart_layers_*`, `scans_per_day_*`, trial/journal admin keys). **SPEC §6.1:** `findRecentSwingLevels` is the canonical S/R (equivalence-tested); `computeRangeLevels` marked removed.
- **SHIPPED markers with version refs:** F1/F1c(E9)/F2/F3/F5/F6/F7/F9/F10/F11/F13/F14/F15/E7b — each tagged with the version it landed in. Charts documented as **in-app SVG** (not recharts) across PRD/SPEC/UX; recharts stays a listed dep (revert-if-preferred decision noted).
- **Open items** rewritten to the HANDOFF follow-ups: resolve-scenarios cron (not auto-run), call-sign persistence from S9, email stubs, fonts, Free 1-scan/day enforcement, admin mobile, POSITION honesty guardrail.
- **Stale-claim sweep:** recharts claims that contradicted the shipped SVG charts fixed; the old P2 scan page marked superseded. The "trial with card" mention in SPEC decision #3 is an intentional change-order record (left as-is).
- **⚠ For Nadav:** two open product calls surfaced (not decided here): (1) CAPITAL SAVES non-passer variant vs the current trigger-never-filled definition; (2) charts as in-app SVG vs recharts. Both documented as open.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## Latest — Package B phase 2: B4 dashboard + B5 profile + B6 academy + B7 admin (v0.9.0, code + migration 028)
- **What shipped:** the whole post-scan shell. **B4** "What Would Have Happened" (F3, the retention core) with real reveal-gating; **B5** profile + rank ladder; **B6** academy shell (12 modules); **B7** admin console (6 sections). Backend pytest **66/66**; frontend builds clean (20 routes).
- **B4 reveal-gating (the AC that matters):** `journal_scenarios` (mig 028) is created from each scan — one `pass` scenario per PASS momentum row, one `no_setups_day` per skip day; **WATCH is never a scenario**. A server-side job (`backend/app/tasks/journal_tasks.py` + `scripts/run_resolve_scenarios.py`) resolves open scenarios against subsequent Bybit daily candles (trigger fill → target/risk/7-day expiry) into an honest hypothetical R. **Outcomes are withheld from every client payload until the user's next scan reveals them** (`core/journal.on_scan` reveals-then-creates); unrevealed rows carry zero outcome data in the payload AND the DOM (regression: backend `test_journal_withholds_outcome_until_reveal` asserts the value/label are absent from the whole response; frontend `scenarioOutcome()` returns null when unrevealed). Nav badge = `/api/journal/badge` count only. +25 XP on viewing a revealed outcome (idempotent per scenario).
- **B5/B6/B7:** profile (`/api/profile`, call-sign in `user_settings` with email fallback, ladder from `levelFor`, Lens/Risk settings persist); academy (12 modules = the 12 `academy` ids in the tooltip JSON, seeded from each term's `what`; +100 only for real-content modules ≥3 terms, stubs award 0; tooltip `/academy#<id>` deep-link highlights the module; plan + rank gating); admin (`require_admin`→403, real-data overview/MRR/churn, user overrides audited to `admin_events`, ticket queue+reply with email-stub, `system_settings` editor with score-gate/card-off shown LOCKED, broadcast→in-app banner that never covers SCAN/disclaimer, notifications log).
- **⚠ DECISIONS MADE THIS SESSION (need Nadav sign-off):**
  1. **CAPITAL SAVES** implemented as a **PASS whose trigger never filled in the window** (capital preserved, no entry) — faithful to the prompt's "scenario per PASS + no-setups only; WATCH never a scenario", but different from the design frame's per-coin non-passer SAVE row (LINK). Extendable to per-coin saves later if you prefer the frame's literal reading.
  2. **Academy "real content" = ≥3 seed terms** → 9 completable lessons (+100), 3 reference stubs (0 XP: volume_basics, positioning_basics, regime_transitions).
  3. **Module titles use colons** (no-em-dash rule) instead of the frame's em-dash titles.
  4. **Admin shows real DB data** (not the frames' SAMPLE); churn is a real-but-placeholder query until exit-survey data accrues.
  5. **New XP source `admin_grant`** (B7 "Grant XP (support)") added to `XP_ECONOMY.md` §1 as an audited admin-only source (not user-earnable, not shown in "How XP is earned").
- **⬜ Gaps / follow-ups:** the resolution job is server-side and needs the cron wired in the deploy (`python -m backend.scripts.run_resolve_scenarios`, daily) — it is NOT auto-run; scenarios stay `open` until it runs. Call-sign is not yet persisted from onboarding S9 (profile owns it with an email fallback; wiring S9→`/api/profile/settings` is a small follow-up). Admin console is desktop-first (no mobile layout). Email sends (ticket reply, broadcast email, day-11) are logged stubs. Fonts still system-fallback (Design round 2).
- **Ready for production?** Not yet — recommend a manual click-through: B4 reveal flow (scan → resolve job → next scan reveals), B7 admin as an admin user, B6 deep-link from a tooltip. Nadav decides the dev→main merge.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## Latest — Package B phase 1: B1 scan + B2 subscribe + B3 nav (v0.8.0, code + migration 027)
- **What shipped:** the full scan screen (B1a–B1g, the product's heart) replacing the P2 page; the Subscribe page (B2); and the post-auth hamburger nav + unified header (B3). Backend gating is now server-authoritative.
- **Shared engine (S/R canon):** ported the personal tool's `findRecentSwingLevels` ({swingHigh,swingLow}) into `@finaroda/scoring-engine` byte-faithfully — it is the one S/R source the scorer measures against. Deleted `frontend/src/lib/onboarding/levels.ts` (the week-old SaaS pivot approximation) and repointed EpisodeChart + the new B1 chart to a shared `swingLevels` adapter. Added an **equivalence test** (shared/scoring-engine.test.js) that asserts identical swings vs a verbatim copy of engine.mjs across many deterministic vectors. Per your decision, the onboarding chart's drawn S/R may shift slightly — intended, so the chart draws exactly what the engine scores.
- **B1 gating (server-authoritative, your decision):** the scan stays client-side, but `GET /api/scan/entitlements` is binding (coins/scan, chart_layers, scans/day per tier from system_settings, mig 027), and `POST /api/scan/events` rejects an over-limit scan (403 PLAN_COIN_LIMIT). First-scan-of-day XP (+50) is credited server-side, idempotent per calendar day (source=daily_first_scan, ref=date); the client only learns whether it was awarded (drives the chip). Chart layers gate EMA7 + Blueprint levels (Free=EMA200 only + SEE PLANS).
- **E7b:** every scanned coin (incl. non-passers) is tappable → the same Chart Standard v1 + a plain-language "why not" line naming only the blocking check (regime = price vs EMA200, else the threshold) with its Concept Tooltip. No score/weight/formula is exposed; snapshot header is timestamped (not live).
- **B2:** 4-plan comparison table (TC-J-002, Free first + always visible), D1 no-card trial CTA + 3 trust shields, "same engine, same threshold" line. Prices/coins come from system_settings via `GET /api/plans`. Legacy `/paywall` now redirects to `/subscribe`; the onboarding trial fork routes to `/subscribe`.
- **B3:** unified header (≡ / FINARODA / LevelMeter chip) that kills the old per-page header; hamburger drawer (Dashboard[UPDATE]/Profile/Academy/Settings) with a LevelMeter identity block; "Report a problem" files a real ticket (`POST /api/support/tickets`).
- **⚠ DECISIONS ALREADY MADE BY NADAV (this session):** (1) gating = server-as-authority + client compute (unpersisted local computation is acceptable leakage). (2) swing canon = the personal-tool algorithm (not the SaaS pivot). (3) journal scenarios from scans **deferred** — F3 plumbing does NOT exist yet; scans persist to score_log + decision_snapshots as before. (4) B7 admin console (settings editor + ticket queue) = phase 2.
- **⬜ Gaps / follow-ups:** F3 journal-scenario creation (score_log→journal) still to be built; scans_per_day is exposed in entitlements but only coins/scan + chart_layers are HARD-gated this phase (Free 1-scan/day not enforced — matches B1c "scan again always allowed"); the empty-state discipline badge shows a real scan count, NOT a fabricated "skipped X of Y days" ratio (no scan_events per-day skip endpoint yet — honest per §8); B7 admin console (edit prices/coins, ticket queue) is phase 2; fonts still system-fallback (Design round 2). Nav routes /profile, /settings are stubs (may 404 until built).
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.
- **Ready for production?** Not yet — recommend a manual mobile click-through of B1/B2/B3 first (charts, tooltips, gating visuals, trial CTA). Nadav decides the dev→main merge.

## Latest — F13 validation round 2 polish (v0.7.1, frontend-focused)
- Applied Nadav's 10 click-through polish items.
- **S0** now has a **LET'S START** button (no auto-advance). **Header** redesigned: the XP bar + gray caption became a compact terminal **LevelMeter** (hexagon rank badge + rank + XP + progress toward the next rank, XP_ECONOMY ladder 1000/3000/8000).
- **Signup flash (reopened):** fixed with a **render-gate** — the flow does not render until the `/me` routing check resolves, so a late async redirect can't yank a mid-flow screen (the concrete cause for a returning/completed user during repeated testing). S5→S6 also consolidated to a single `createOnce` transition. Unit-tested.
- **Tooltip context guard:** `renderNow` now suppresses the whole `now` line when a simple placeholder is missing (long_short shows nothing before a choice is made; fixes the premature-direction glitch). Unit-tested.
- **Chart:** symbol is a prominent separate badge (all charts incl. S10); Trigger/Risk/Target + EMA/S-R labels are decluttered (no overlap). **S8:** pre-scan framing line ("Real case: ADA, 25 Jun 2026. Press SCAN..."), the verified **Calculated Risk Level 0.1511** is seeded + drawn, and a **Why PASS** popover lists the stored passed checks (regime/weekly/timing/volume) using the locked concept labels.
- **XP = LEVEL framing** with a **level-up celebration** (distinct vibration + terminal animation) that fires ONLY on rank crossings; ordinary XP gains get the subtle E6 buzz. Onboarding tops out at 300 (Level 1) so the celebration is dormant here but implemented + unit-tested.
- **S10 copy** made unambiguous ("revealed on your NEXT scan"). **S11 table** wrapped in overflow-x synced to the frame.
- **New frontend unit-test harness:** `frontend/tests/*.test.ts` via `node --test --experimental-strip-types` (`npm run test:unit`), covering level math, tooltip suppression, and the transition guard. `allowImportingTsExtensions` added to tsconfig for the `.ts` test imports.
- **⚠ Carried-over, still open for Nadav:** EpisodeChart is in-app SVG (not recharts); Google/Apple OAuth SDK is round-2 (magic-link fully wired, closes the loop in dev); E1 = BTC (LINK was a curation error). Fonts (IBM Plex Mono / Space Grotesk) are referenced via CSS font-family but not yet self-hosted/imported — they fall back to system mono; loading them is a Design-round-2 item.
- **⬜ Still pending:** manual mobile click-through (header, tooltip clipping, chart interactions, level-up visual). 
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## Latest — F13 validation round 1 + tooltip content (v0.7.0, code + migration 026)
- Applied Nadav's click-through notes + wired the Concept Tooltip content + built Chart Standard v1.
- **Tooltips:** `concept_tooltips_content.json` (root, locked, 46 terms) is now wired. `concepts.ts` loads it + a `now`-template engine (`{key}` and `{cond:'a'|c2:'b'}`, missing placeholder renders empty). Bubble is viewport-clamped/flips (never clipped on mobile), has an X and closes on tap-outside. A pytest drift-guard asserts the bundled frontend copy matches root.
- **Bug fixes:** (a) **XP anti-farming** — onboarding XP is now a single lifetime grant (300) credited at completion; migration 026 adds a partial unique index on `xp_events(user_id) WHERE source='onboarding'`; replaying grants 0 (regression test). (b) **Routing** — /onboarding guards on `/me` and `router.replace('/scan')` for completed users; S11 exit uses replace; back never re-enters. (c) **Signup flash** — removed a side-effect-in-state-updater anti-pattern + added a `signingUp` guard = one clean S5→S6 transition. (d) Live **"new scan"** returns to the controls (idle), not an immediate re-scan. (e) Line-orphan control via `noOrphan()` + CSS text-balance.
- **Product decisions (Nadav 12/07):** BUY/SELL → **LONG/SHORT**. **Two different S1a branches, both real candles:** LONG = the fade (−10%), SHORT = the **squeeze** (+2.06% against the short to 65,624, then fade). E1's decision candle was moved to the 20 Jun surge so both branches are empirically true; the seed builder asserts squeeze ≥1.5% and fade ≤−5%. **No-em-dash copy rule** enforced across all copy + a pytest lint that fails on U+2014.
- **Chart Standard v1** (one component, the Package B base): on-chart context header, EMA200+EMA7 with labels, swing S/R (`computeRangeLevels`, pivots), Blueprint levels (S8), Spike/Entry annotation pills that open tooltips, candle-tap OHLC.
- **⚠ Carried-over decisions still open for Nadav** (from v0.6.0): EpisodeChart is in-app SVG (not recharts); Google/Apple OAuth SDK is round-2 (magic-link fully wired, closes the loop in dev); E1 = BTC (LINK was a curation error).
- **⬜ Still pending:** manual mobile click-through (tooltip clipping, chart interactions, the 12-screen flow); visual polish (fonts/animations) = Design round 2.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## Latest — F13 onboarding implemented (v0.6.0, code + migrations 023–025)
- **What shipped:** the full "First 60 Seconds" onboarding — episode engine, 12 screens (S0–S11) + the S1a failure branch, XP, funnel, server-side outcome withholding. Backend **pytest 39/39**; frontend **builds clean**.
- **Episode engine:** `episodes` table (mig 023) seeded from **real Bybit daily klines** (`backend/data/onboarding_episodes.json`) with **empirical-truth assertions in the builder** — it throws if the real klines don't support the documented entry/outcome. Each candle carries real EMA7/EMA200. Charts render **in-app as SVG** from stored klines — never external captures.
- **Withholding (AC):** `GET /api/onboarding/episodes/{id}` returns only the pre-decision setup candles; the outcome (win/loss, R, %) and reveal candles are returned ONLY by `POST …/{id}/reveal` (S1 trap, S10 time-machine). Verified by test that the withheld numbers are absent from the setup payload.
- **XP:** `xp_events` (mig 024) — amounts are server-authoritative (closed map 50/100/50/100 = 300), award is idempotent via `UNIQUE(user,source,ref)`. **Funnel:** `onboarding_funnel_events` (mig 025), anon before signup / user after. `POST /complete` marks `onboarding_completed_at`.
- **⚠ DECISIONS THAT NEED NADAV'S SIGN-OFF:**
  1. **E1 trap re-picked LINK → BTCUSDT 25/06.** The original LINK 30/06→02/07 window actually *rose* in real klines (LINK's fade came after 02/07 — a curation error), which would have broken the trap lesson + violated empirical-truth. Per your call I used the 25 Jun BTC anchor-DIR-FAIL (61,753 → 57,802, −6.4%; green spike 06-22 → fade), which is our own verified material. LINK is now history-only in the docs. Documented in `EPISODES_AND_VERIFIED_NUMBERS.md`.
  2. **EpisodeChart uses an in-app SVG candlestick, NOT recharts** (the spec named recharts). Reason: recharts has no native candlestick and adds a React-19 peer-dependency risk to the Vercel build; SVG is the spec's own "v25.67 engine" reference, zero-dep, real klines. Same visual outcome. Revert to recharts if you prefer.
  3. **Concept Tooltip definitions are placeholders** (keyed by term id) — you supply the 35+ plain-language definitions from the internal guide; I did not write financial definitions.
  4. **S5 signup:** magic-link path is fully wired (in dev it closes the loop in-session so XP/completion persist); Google/Apple are present as buttons but full OAuth-SDK wiring is the round-2 auth bundle.
- **⬜ Still pending:** manual runtime click-through of all 12 screens (needs both servers + browser); visual polish (Space Grotesk / IBM Plex Mono fonts, animations) = Design round 2; Google/Apple OAuth SDK.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, scoring engine/scorer, calculator terminology, main branch.

## Latest — DOCS XP economy anchored + debt closed (v0.5.3, docs only, no code)
- Anchored **`XP_ECONOMY.md` v1.0** (locked, repo root) into the source-of-truth docs; closed the XP-economy debt (Onboarding §8 / ALIGNMENT D3). **No app/engine/scorer/backend change.**
- **UX §5 + PRD F5 — tier contradiction fixed:** the "מדייק"/Precise tier no longer keys on "positive what-if history" (that rewarded outcomes → broke trust-not-engagement). Now **XP threshold only**; what-if quality stays a **dashboard statistic**, never a tier criterion. Explicit correction note cites `XP_ECONOMY.md` §4.
- **PRD F6 (Academy):** links `XP_ECONOMY.md` — +100 XP/lesson; XP ranks unlock **bonus knowledge modules** (Spike Autopsies, Regime Transitions) **orthogonal to plan gates**. New AC4.
- **SPEC §5.6 (new):** `xp_events (user_id, source, ref, amount, ts)` with `UNIQUE (user_id, source, ref)` (idempotent farming-guard), closed-list sources, **server-side write only**, ranks derived from `SUM(amount)` (no stored rank column).
- **Onboarding §4/§8:** §8 marked **✅ closed → `XP_ECONOMY.md`**; §4 points to the closed XP sources (+50 first daily scan · +100 lesson · +25 journal reveal · no streak in v1).
- **ATP:** +TC-DOCS-XP01 (doc-check).
- **⚠ FLAG for Nadav (scope):** the instruction named only **UX §5** for the tier fix, but the identical "positive what-if history" contradiction lived in **PRD F5** too. I fixed both — leaving PRD F5 would keep a RED-LINE contradiction between two source-of-truth docs. If you'd rather I'd stayed strictly in UX §5, say so and I'll revert the F5 edit.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, client-side fetch, calculator terminology, engine/scorer/backend. `xp_events` migration ⬜ pending (P3/P4).

## Latest — DOCS E9 Horizon selector (v0.5.2, docs only, no code)
- Applied **E9** of `ALIGNMENT_2026-07-09.md` to the source-of-truth docs. **No app/engine/scorer/backend change.**
- **New PRD F1c — Horizon Selector:** pre-scan control. **SWING (1–7 days) active** in v1 (the verified EMA7-slope edge is a swing edge). **POSITION (weeks+) locked** — approved copy "In validation. Unlocks when it earns it." + tooltip. Unlock = 30+ outcomes / 2+ regimes; candidate Pro feature. POSITION engine → PRD V2 backlog.
- **UX §3 + §8:** Horizon added to the pre-scan controls row (next to Lens/Risk Style). Documented that POSITION is a **separate validated universe**, not a cosmetic toggle; added a minimalism ceiling note (3 controls).
- **ROADMAP X1:** Horizon selector added to Design round-2 scope. **ATP:** +TC-DOCS-E06.
- **⚠ OPEN — honesty guardrail (needs Nadav's call):** the approved tooltip claims the position engine "is being validated against live outcomes" — but **there is no position engine** (the only verified edge is swing/EMA7; nothing logs position outcomes). Per Principle 8 (empirical truth in copy) this claim isn't currently true. Baked into **F1c AC2 + TC-DOCS-E06**: copy is truthful only once a position-outcome log actually exists; until then POSITION is "planned," not "in validation." Also flagged: by the 30+/2+regimes criterion, position outcomes resolve over weeks → ~multi-year to unlock → **POSITION may never unlock; no ETA promised.** Decision needed: (a) build a position-outcome log before showing the copy, or (b) soften the copy to future-tense.
- **Untouched (as required):** RED LINE §3.5.5 (preserved per-universe), 85/82 threshold, client-side fetch, calculator terminology, engine/scorer/backend.

## Latest — DOCS section-E alignment (v0.5.1, docs only, no code)
- Applied **section E** of `ALIGNMENT_2026-07-09.md` (Nadav's 2026-07-11 product decisions) to the source-of-truth docs. **No app/engine/scorer/backend change.**
- **E1 — Concept Tooltip:** new **PRD F14** (F-education) — one shared learn-bubble on every term from onboarding onward, content sourced from Academy (F6), display-only. UX §3 + §8.
- **E2 — free-coin analysis:** added to PRD **V2 backlog only** (paid plans, after validation + Nadav approval, mandatory "Learning mode — outside the validated universe" label). Not v1.
- **E3 — comparison table on Subscribe:** PRD F7 + UX §6 — the Free-vs-paid table ("Free forever": 1 scan/day · 2 coins · full Blueprint · journal 7 days · no export) MUST render on the Subscribe page. New paywall AC + **ATP TC-J-002** copy-guard.
- **E5 — post-payment hamburger nav** (Dashboard/Profile/Academy/Settings): into Design round-2 scope (ROADMAP X1). UX §8.
- **E6 — SCAN vibrate:** SPEC §6.2 — `navigator.vibrate` with silent fallback (iOS Safari unsupported → no buzz, no error).
- **E7 — Live Chart + overlays per coin:** new **PRD F15** + UX §3 — recharts-from-kline chart with explanation layers; gating Free = chart+EMA200 / paid = all layers (EMA7 + Blueprint levels). Design round-2. display-only.
- **E8 — running ticker banner: REJECTED** — recorded in PRD §17 (new "פריטים שנדחו" subsection): conflicts with trust-not-engagement + index/commodity data licensing; alternative = existing static per-scan marketContext line; reopen only as an off-by-default opt-in.
- **E4** (leveraged-ETF research) — personal-tool research track only, **does not touch SaaS**; SaaS docs deliberately unchanged.
- **ATP:** +TC-J-002 (E3 copy-guard) + TC-DOCS-E01..E05 (E01-E03 ⬜ pending implementation; E04/E05 ✅ doc-check).
- **⬜ Still pending (implementation):** F14 tooltip, F15 live chart, the Subscribe-page table, SCAN vibrate, hamburger nav — all land under ROADMAP **X1** (Design round-2) / **P4**.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, client-side fetch, calculator terminology, engine/scorer/backend.

## Latest — D1 trial WITHOUT card implemented (v0.5.0, code + migration 022)
- **Model change (SPEC §9/§12.3, PRD F7):** trial no longer needs a card. Card capture happens ONLY at explicit paid conversion (`initiate_checkout`, Cardcom LowProfile). Still TEST mode (`FEATURE_CARDCOM_LIVE=false` → 503 on initiate, dry-run renewal).
- **`start_trial`** (`core/cardcom_service.py`): no card/tokenization; `next_billing_at=NULL`; default plan `pro` (full 14-day Pro). Re-trial → 409.
- **`expire_trials`**: trial end → **Free** (`tier='free'`, `subscription_status='none'`, `next_billing_at=NULL`) — never expired/blocked, never charged. New event `trial_ended_to_free`. Returns `{"moved_to_free": N}`.
- **`run_renewal_batch`**: narrowed to `subscription_status='active'` — trials are never billing candidates.
- **Day-11 reminder:** `trial_ending_soon_task` fires `TRIAL_REMINDER_LEAD_DAYS` (default 3) before end (day 11 of 14). New config `TRIAL_REMINDER_LEAD_DAYS`. railway.toml comment day-13→day-11.
- **Migration 022** (`022_subscription_events_trial_to_free.py`): rebuilds `subscription_events` to add `event_type='trial_ended_to_free'` to the CHECK (SQLite can't ALTER a CHECK). Table count unchanged.
- **Frontend:** `paywall/page.tsx` copy fixed ("no credit card / card only at paid plan / continue on Free"); button "Start trial"→"Choose plan".
- **Free representation:** `tier='free' + subscription_status='none'` (the existing free state) — no users-table rebuild needed. `'free'` is not a valid `subscription_status` (CHECK), by design.
- **Tests:** TC-F-005/006 rewritten (no-card trial + Free downgrade), TC-F-007 (re-trial 409), TC-F-008 (renewal never charges trials). ATP TC-DOCS-001/002 → ✅.
- **⬜ Still pending for P4 (ROADMAP S3):** live plans + full paywall, **Free-tier limit enforcement** (1 scan/day, F3 7-day window, export gate) from `system_settings`, coupons, referral. This task did the trial/billing spine only.

## Latest — DOCS alignment to ALIGNMENT_2026-07-09 (v0.4.8, docs only, no code)
- Aligned all source-of-truth docs to Nadav's 2026-07-09 decisions. **No app/engine/scorer/backend change.**
- **D1 — Trial WITHOUT card** (approved change order, supersedes the locked "trial with card"): SPEC §9/§12.3, PRD F7 (flow + AC1/AC2 rewritten) + §17, UX journey step 4 + §6, LEGAL §2.6. 14 days · no tokenization at signup · no auto-charge · day-11 reminder · active choice at end (paid plan or Free) · card capture moves to the paid-conversion moment.
  - ⚠ **Code still tokenizes:** `start_trial`/renewal in `core/cardcom_service.py` are UNCHANGED and still set `next_billing` (card-based). Reworking them is **⬜ pending implementation** under ROADMAP **S3** (not done in this docs task). TC-F-005/006 still reflect the old code (✅); new spec is TC-DOCS-001/002 (⬜).
- **D2 — Free tier** (new, approved): Free row in PRD F7 / SPEC §9 / UX §6 — 1 scan/day · 2 coins · full Trading Blueprint · F3 limited to last 7 days · no export · basic academy, all `system_settings`-controlled. Paywall/fork copy: secondary "Continue on Free". SPEC §12 decision 8.
- **D3 — XP:** OPEN, **not implemented** — added to PRD §17 open items (conflict with UX §9.4; proposed +XP on first scan of the day only).
- **F13 onboarding simulation** registered in PRD (spec = FINARODA_ONBOARDING_SPEC.md v1.1, complements F12); ROADMAP X1 gains the 12 onboarding screens.
- **F3 reveal-gating (B3):** outcome revealed on the next scan + "journal has an update" teaser (pull-not-push).
- **Schema notes (docs only, not migrated):** SPEC §5.2 future `score_log.regime_state TEXT` (bear/bull/transition, BTC, N=5 hysteresis); SPEC §5.5 new `episodes` table (real dated kline, recharts render, never TradingView/Bybit). PRD §3 principle 8 — empirical truth in copy.
- **Untouched (as required):** RED LINE §3.5.5, 85/82 threshold, client-side fetch, calculator terminology.

## 🟢 STAGING — LIVE (deployed from `dev`)
- **Frontend (Vercel):** https://finaroda-saas.vercel.app  — Root Directory `frontend`, pnpm workspace, Production Branch = `dev`.
- **Backend (Railway):** https://finaroda-saas-production.up.railway.app  — nixpacks Python, volume `/app/data`, plain uvicorn (no Litestream). `ENVIRONMENT=staging`.
- **Cardcom:** TEST mode (`FEATURE_CARDCOM_LIVE=false`) — no real charges.
- **Login on staging (no email):** `DEV_RETURN_MAGIC_LINK=true` + `FEATURE_PUBLIC_SIGNUPS_OPEN=true`. To log in: `POST {backend}/api/auth/magic-link {"email":"..."}` → the response's `dev_magic_link` contains the token → open it (or `GET {backend}/api/auth/verify?token=...`) to set the `access_token` cookie. `rodanis@gmail.com` is a bootstrap admin (is_admin=true).
- **Smoke test (2026-07-01, all PASS):** health 200 (env=staging) · magic-link→verify→/me authenticated as rodanis (is_admin) · authenticated `POST /api/scan/events` → 200, persisted `scan_event_id=1` + `score_log` BTCUSDT · unauthenticated `/api/scan/events` → 401 (expected, auth-required — not CORS).
- **Not deployed:** Litestream/R2 backups (skipped for staging), Resend email (dev-return link instead), production domain (finaroda.com), lawyer/accountant sign-off.

## מה נעשה בסשן האחרון (P1 — תשתית חיה)
- **Auth (מוקשח, SPEC §4):** magic-link (Resend, console fallback בdev) + Google OAuth (iss תמיד, aud כשיש CLIENT_ID) + Apple stub(501). JWT ב-httpOnly cookie + get_current_user. magic-link token נשמר כ-**SHA-256 hash**. admin כ-role ב-DB (users.is_admin) עם bootstrap לפי ADMIN_BOOTSTRAP_EMAILS. beta gate + allowlist + waitlist. endpoints: /api/auth/{magic-link,verify,google,apple,logout,me} + /api/waitlist.
- **Cardcom v11 (TEST mode בלבד):** initiate(LowProfile)/webhook(HMAC)/status/cancel + charge_recurring(ChargeToken) + start_trial(14 יום, כרטיס) + run_renewal_batch + expire_trials. מיפוי basic/advanced/pro, מחירים מ-system_settings. **הכל dry-run/503 עד FEATURE_CARDCOM_LIVE=true.** migration 019 (billing_failure_count + מחירי פלאנים).
- **Deploy (מוכן, לא פרוס):** railway.toml (+3 crons), nixpacks.toml, litestream.yml (finaroda.db→R2), Dockerfile.
- **Frontend:** src/lib/api.ts + login/verify/coming-soon/paywall/checkout(success,cancelled) מחוברים ל-endpoints. UI אנגלית.

## מה עודכן ע"י נדב (לא Claude): scoring-engine
- **🟢 levels engine חולץ** מהכלי האישי (v25.80) ל-`shared/scoring-engine.js`: calcEMA/RSI/ATR/ADX/closedCandles/ema7Slope/computeSlTp/computeReversalAnchor — byte-faithful, node --test 8/8.
- **🟡 scoreDirection עדיין stub שזורק** — pass 2, דורש golden vectors מהכלי האישי (ראו `shared/scoring-engine.api.md`).

## Latest change — DOCS regulatory reframing (docs only, no code)
- Reframed the FRONT-END as a utility calculator. **Engine/score/edge/threshold unchanged.**
- New calculator terminology (Mathematical Trigger Point / Calculated Risk Level / Calculated Target Level / Dynamic Risk Level / Trading Blueprint), formula-transparency notes, an **Analysis Lens** (display only) and a **Risk Style** (Conservative/Balanced/Aggressive → `computeSlTp` opt only).
- **RED LINE:** client never changes score/weights/edge/85-82 threshold — only what's displayed + risk geometry. Authoritative spec: **PRD §3.5**; legal spine: **LEGAL §6**.
- **One product-scope change:** the per-user "personal threshold" was removed (violated the RED LINE). `users.default_threshold` stays for admin-only per-user overrides.
- Docs: PRD v2.1, UX v1.2, LEGAL v2.

## Latest — P2 scorer wired (real score live, dev only)
- **Real scorer wired:** `scoreDirection` (momentum profile = `MOMENTUM_CAL`) imported from `@finaroda/scoring-engine/scorer.js` (verbatim v25.80, untouched). `SCORE_GATE_ENABLED=true` — the live **85 PASS / 82-84 WATCH** gate on the numeric score; a `blocked` macro-gate result is hidden.
- **Direction:** score both long+short (momentum); prefer non-blocked, then higher score.
- **3 profiles logged (measure-first):** momentum (displayed) + pullback (`DEFAULT_CALIBRATION`) + continuation (`{...DEFAULT_CALIBRATION, entryMode:'continuation'}`) → `score_log` (migration 021 added `profile`). Only momentum is displayed.
- **RED LINE kept:** score uses fixed inputs (`DEFAULT_RISK`+`MOMENTUM_CAL`); Risk Style feeds only `computeSlTp` opt (levels). Analysis Lens is now display-only (no longer gates).
- **marketData extended client-side:** weekly (derived), open interest + oiChangePct (best-effort), change24h; marketContext (coinChanges/mean/std) built per scan.

## P2 scan core (base, done, dev only)
- **Scan flow:** central SCAN button + Lens/Risk-Style toggles → streaming log (4 steps) → client-side Bybit fetch (user IP, no shared cache; thin `/api/market/proxy` CORS fallback) → passers as circles (ring ≤5 / list >5) → **Trading Blueprint** → empty state (F1b).
- **Engine imported** as `@finaroda/scoring-engine` (symlink to `shared/`, `transpilePackages`, ambient d.ts). Levels only: computeSlTp / computeReversalAnchor / ema7Slope / indicators. **scoreDirection never called.**
- **Score-pending:** every coin recorded with `score=NULL` (migration 020); card shows "Score pending — engine pass 2 (levels are real)". Real 85/82 gate wired behind `SCORE_GATE_ENABLED=false` in `engine.ts`.
- **Interim rules (documented):** direction = sign of verified EMA7 slope; visibility = levels valid + Analysis-Lens condition (`lens.ts`).
- **Persistence (SPEC §5):** `POST /api/scan/events` (scan_events + score_log) and `POST /api/scan/snapshot` (decision_snapshots), auth-required, best-effort from the client.
- **RED LINE honored:** Lens = display only; Risk Style = `computeSlTp` opt only; score/weights/edge/threshold never client-touchable.

## Next step — P3 (learning loop)
- **Score is live** — no longer blocked (scorer extracted + wired).
- **P3 (SPEC §11):** backtest cron over `score_log` (outcome NULL) → "what would have happened" dashboard (F3). The 3 profiles now in `score_log` enable base-rate per profile.
- **Still open:** per-plan coin limit (2/5/10 from system_settings) not yet wired into the scan universe (currently a fixed 10-coin default). Open-interest/marketContext are best-effort — revisit accuracy in P3.

## מוכן לפריסה (ממתין להפעלה ידנית של נדב)
- **Cardcom live:** להזין credentials אמיתיים (sandbox→prod) + FEATURE_CARDCOM_LIVE=true. עד אז אין חיוב.
- **Railway deploy:** לחבר repo, למפות volume ל-/app/data, להזין env vars (backend/.env.example), להפעיל. CI (pytest+tsc) — עוד לא נכתב (אפשר ב-P2+).
- **Resend/Google/R2:** להזין מפתחות אמיתיים ב-env הפרודקשן.

## פתוחים / חוסמים
- אישור PRD מנדב; אישור עו"ד ל-LEGAL; אימות מחירים מול רו"ח; פלט Claude Design ל-UX.
- scoreDirection pass 2 (golden vectors) — חוסם ציון אמיתי ב-P2.

---

## תזכורת Branch Gate
Claude עובד על dev בלבד. נדב ממזג dev→main ידנית. אסור ל-Claude לגעת ב-main/production.

## איך מריצים מקומית
- Backend: `.venv/Scripts/python.exe -m uvicorn backend.main:app --reload --port 8000`
- Frontend: `cd frontend && pnpm dev`  (צריך `NEXT_PUBLIC_API_URL`, ברירת מחדל localhost:8000)
- Validation: `.venv/Scripts/python.exe -m pytest` · `cd frontend && pnpm exec tsc --noEmit && pnpm run lint` · `cd shared && node --test`
